async function exchangeLoginToken(token){  const status=document.getElementById('login-status');  status.textContent='检测到登录 token，正在交换会话…';  try {    const response=await fetch('/api/login/exchange',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:token}),credentials:'same-origin',cache:'no-store'});    let data={};    try{data=await response.json();}catch(_error){}    if(!response.ok||!data.ok){throw new Error(data.error||data.message||'登录失败');}    history.replaceState(null,'','/');    window.location.href='/';  } catch (error) {    status.textContent=(error&&error.message)?error.message:'登录失败，请重新在日志群发送 .登录';  }}document.addEventListener('DOMContentLoaded',function(){  const status=document.getElementById('login-status');  const hash=new URLSearchParams(window.location.hash.slice(1));  const token=hash.get('token');  if(token){exchangeLoginToken(token);return;}  status.textContent='未检测到登录 token。请在日志群发送 .登录，并打开机器人回复的链接。';});
