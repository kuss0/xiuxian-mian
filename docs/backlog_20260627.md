# 2026-06-27 待办账本

记录口径：
- `待办`：尚未实现或尚未确认。
- `进行中`：已经开始改动或审计。
- `已销号`：必须附带代码位置、测试结果、上线观察或提交号。
- 不依赖聊天上下文保存需求；新增需求先入账，再按优先级处理。

## 接手指引

接手环境：
- 主仓库：`/opt/xiuxian-main`
- 当前分支：`main`
- 生产目标：`xiuxian-mian/main`
- 最近已上线提交：`f3cf431 Do not probe on suspected game bot replies`
- 当前状态：`xiuxian.service`、`xiuxian-listener.service`、`xiuxian-safety-watchdog.service`、`xiuxian-health-observer.service` 均 active；`data/state/chaogu_state.db` 中 `meta.global_enabled=0`、`global_pause_source=bot_health_monitor`，维护期保留 pending `.登天阶` 与 bot-health probe `.我的灵根` 证据；`xiuxian-listener` 进程 active 但 sidecar 心跳为 `degraded_no_connected_accounts`，原因是 `listener_account_*.session` 未独立授权；未见 `data/state/safety_watchdog_fused.json`。

协作规则：
- 新需求先追加到账本，再开始改代码；销号时必须写清代码位置、测试命令、上线或观察证据。
- 不要修改旧目录 `/opt/xiuxian`、`/opt/xiuxian-c`，除非用户明确要求。
- 不要回滚或覆盖无关改动；发现脏工作区先确认来源，只处理本任务相关文件。
- 涉及游戏主动发送的逻辑，优先被动解析和带冷却/去重/安全锁的调度；不要为了“补偿”直接加高频轮询。
- 高优先级通知使用现有 `send_audit_log(..., priority="high")` 路径；不要绕过全局发送、安全锁、看门狗。
- 不要移除或放宽安全锁、看门狗、全局恢复约束，除非有明确测试覆盖并能解释不会重复发送。

上线前最低验证：
- `.venv/bin/python -m pytest -q`
- `.venv/bin/python -m compileall -q model tests`
- `git diff --check`
- `systemctl is-active xiuxian.service`
- `systemctl is-active xiuxian-safety-watchdog.service`
- `sqlite3 data/state/chaogu_state.db "select key,value from meta where key='global_enabled';"`
- `.venv/bin/python tools/health_observer.py --project-root /opt/xiuxian-main --once`

当前剩余重点：
- `小世界短窗重复查询观察`：2026-06-27 18:59 上线后健康观察仍报 30m 窗口内 `8659059191:.小世界 x4`，真实发送发生在 18:39-18:42、早于 `cf7b37e` 上线；当前 DB 显示该身份小世界 `idle`。按顺序先观察，不插队修复。

观察补记（2026-06-28 13:30）：复查 `data/messages/2026-06-28.log`，今天脚本主动 `.小世界` 查询共 5 条，WA 的短间隔只出现在维护链内复查：00:45:08 -> 00:46:40、06:48:10 -> 06:49:15；12:50:24 后直接 `.显灵`，未再重复 `.小世界`，也未显灵前多余 `.收割香火`。live DB：`301299112` 与 `8659059191` 小世界 phase 均为 `idle`，query/manifest/harvest msg id 均为 0；`301299112` 正等待赈灾神迹 CD，`8659059191` 下次时间在下一轮约 6h 后。结论：未见当前版本仍存在短窗重复查询；继续运行观察即可，不做代码改动。

常用入口：
- 副本/落云/黄龙：`model/app_replica.py`
- 钓鱼行为与运行态：`model/features/fishing_behavior.py`、`model/features/fishing_runtime.py`
- 调度接入与被动处理：`model/app.py`
- 身份等级解析：`model/identity_levels.py`
- 关键测试：`tests/test_replica_absorb.py`、`tests/test_fishing_runtime.py`、`tests/test_app_scheduler_contract.py`、`tests/test_second_soul.py`
- 近期真实日志参考：`data/messages/2026-06-26.log`、`data/messages/replica-2026-06-26.log`

## 当前批次

### 2026-07-10 MiniApp 现场沉淀

- 世界 Boss MiniApp：先暂停继续深追自动入场/出手。已确认新版开场只做提醒和候选统计，候选应按登录账号去重且最多 4 个，当前 live 候选为 `wa2000`、`吧唧`、`Lsfnqy`、`Wise Mole`；旧 `world_boss_enabled=0` 不应导致 MiniApp 候选为空。已抓到过期/锁房后的 `/start`/`/state` 形态：锁房后 `/state` 仍可返回 boss 状态，事件关闭后会返回 `boss_token_missing`/`boss_event_closed`。后续若继续抓包，观察器必须按日期滚动读取 `data/messages/YYYY-MM-DD.log`，不能固定当天文件导致跨日漏抓。
- 洞府 MiniApp：用户提供通用 `df_*` 入口已只读验证，不依赖游戏群命令也可打开 MiniApp。`RequestMainWebView` 成功返回 `asc.aiopenai.app/miniapp/xianxia-verify` 与短 TTL initData，`POST /api/miniapp/xianxia-dwelling/start` 返回 200，顶层包含 `account`、`dwelling`、`identity`；这是洞府总控面板，不只是寻宝。寻宝次数在 `dwelling.hunt`，样本为 `used=0`、`limit=3`、`remaining=3`、`actionPoints=8`；闭关/深度闭关状态在 `dwelling.meditation.standardCultivation/deepSeclusion`；天机试炼、观星台/灵圃等入口可能在 `account.externalApps.groups[].apps[]`，只允许保存 start kind/digest 等脱敏摘要，不保存 raw URL/token/initData。7/10 已用 WA 实测寻宝 3/3 跑通并清洗 capture；后续洞府闭关只能先只读解析和边界审计，不能绕过现有深度闭关/天星推命状态机直接实发。

| 状态 | 事项 | 验收口径 | 销号记录 |
| --- | --- | --- | --- |
| 已销号 | 2026-07-18 五子同心魔自动镇魔阈值降至 60 | 将第二元神自动镇魔触发阈值由当前值调整为魔染 `>=60`；同步现有配置默认值、UI 回显、迁移口径和测试，确认不会在未知/未读取魔染时误发。 | `model/features/second_soul.py` 改为每身份可配置阈值，默认与旧库迁移值均为 `60`，仅真实解析到魔染且 `>=阈值` 时触发，`59` 和未知魔染均不镇魔；`model/ui.py` 与模块设置 UI 支持 `1-100` 保存和回显，SQLite module state 新增持久化列。验证：focused `96 passed, 5 subtests passed`，全量 `3211 passed, 396 subtests passed`，compileall、JS 语法、diff check、UI smoke 16 checks、health observer 均通过。提交 `f527a9aa` 已推送并上线；生产库 24 个身份均迁移为 `60`，三服务 active，重启后无额外镇魔/查询。 |
| 已销号 | 2026-07-18 收割香火改走 MiniApp 8 小时周期 | 审计现有 `.收割香火` 命令链失效原因；成熟公共入口身份改为 MiniApp 每 8 小时最多一次，按真实回包校准成功/CD/资源变化，不通过高频状态请求探测，不突破 MiniApp 90 次/分钟和小世界资源约束。 | 新增独立“香火收割 8h”公共入口开关，不开启小世界总 MiniApp 开关；每个可选 `playerId` 独立调度，不按登录账号去重。到期只执行 `small-world collect`，成功或确认当前无可收香火后排下个 8 小时，入口/HTTP/面板异常 30 分钟保守重试；旧小世界链保留显灵/布道等，但不再发送 `.收割香火`。离线验证：全量 `3173 passed, 396 subtests passed`，UI smoke 16 checks ok，JS/compileall/diff check 通过。线上验收：吧唧 MiniApp 收割 4170、库存 28451、下次 14:46:56；WA 收割 8062、库存 40836、下次 14:47:31；消息日志无脚本 `sent .收割香火`，重启后无重复，pending=0，watchdog/health 均正常。提交 `29a89337`、`8cecb51c`。 |
| 已销号 | 2026-07-14 洞府命令/公共入口双轨纠偏 | 最新口径覆盖下方历史记录中的“`.洞府` 已废弃”描述：`.洞府` 群命令入口、入口诊断和手动接管继续保留；公共入口 `df_*` 是独立 HTTP/WebView 链路，频道身份发言关闭时由它承接，频道恢复后两轨仍共存且开关独立。公共入口允许频道身份按 `playerId` 切换，但身份仅因 `channel_send_as_health` 冻结时才可越过普通 `enabled` 门槛，人工停用仍拒绝。入口支持多 URL 健康切换，只有入口/会话健康失败可切备用，业务 CD、次数已尽、今日完成不得跨入口重跑；所有动作全局串行并遵守 90 次/分钟限制，小世界继续不走公共入口。 | 已恢复 `cave_treasure` 的 `.洞府` 诊断/手动白名单，公共入口增加双 URL 候选。线上 20 个频道身份仍因游戏群关闭而冻结发言，公共入口已开启观星台、钓鱼、天机试炼、天机阁元婴、深度闭关和寻宝，小世界保持关闭；冻结频道身份已实测通过公共入口完成元婴出窍与闭关状态同步，未发送 `.洞府`。 |
| 已销号 | 2026-07-14 共享发送/回包冻结与影子观测五项收口 | 作为 CommandAttempt Gate 4 前的上位约束逐项销号：1. 共享发送/回包/全局锁/listener 路由冻结观察 24h，除重复发送、状态腐化或高风险误放行外不再叠补丁；2. 建立只读分模块延迟基线，至少覆盖斗法、野外、慕兰的发送→首回→最终编辑 P50/P95/P99，等待线后续按证据分模块调整，超时先回捞不自动补发高风险命令；3. listener sidecar 不再半残含糊，需授权独立只读监听账号或明确降级/关闭告警；4. CommandAttempt 继续 shadow-only，满 72h、parity/bind/容量/blocked/send_unknown 样本复核通过前不得进入 Gate 4，不得接管发送、补发、CD、reducer 或调度；5. 防御监测保留业务语义，不只看进程，重点覆盖每日一次重复、斗法目标 CD、天星推改消费、小世界信仰消耗、MiniApp 90 次/分钟和频道身份冻结恢复。每项销号必须附代码/工具位置、只读报表或测试、线上观察证据。 | 第 1-4 项证据见 `docs/audit/shared-send-reply-checkpoint-20260716.md`。2026-07-18 第 5 项补 `tools/business_semantic_report.py`：只读脚本发送根消息、Bot 回包和脱敏 MiniApp capture，不发命令、不调用 Telegram/HTTP。最近 3 天 MiniApp 真实 HTTP 2608 次，最密集 60 秒 48/90，无饱和窗口；另排除 202 条世界 Boss `*_business` 非请求记录，避免双算。小世界 21 个脚本面板形成 16 段信仰差值，均没有完整同身份命令/点名天灾证据精确解释，其中 WA 最近为 `98→86`，此前显灵应到 100；这些保留为业务观察样本，不伪称已解释。focused `176 passed, 10 subtests passed`。五项建设完成并销号，但 CommandAttempt 仍严格 shadow-only，Gate 4 继续关闭。 |
| 已销号 | 2026-07-16 用户名变更导致跨模块关系断链 | 用户名可继续变更，业务关系不能依赖写死名称。身份层需以 Telegram 数字 ID 为主键，自动保存历史用户名并持久化；游戏仍返回旧契约名时，合欢等关系链必须解析回当前身份。 | 已在 `model/state.py` 增加自动历史用户名别名，在 `model/persistence.py` 持久化为 `identities.username_aliases`，合欢观察状态新增 `last_partner_identity_id` 并优先按数字 ID 定位锚点。WA 当前名保持 `WalterWA20000`，旧名 `WalterWA2000` 只作为历史别名。真实验收：02:16:18 WA 自动锚点 `175505`，02:16:32 Wise `.双修 温养` `175518` 回复该锚点，02:16:40 游戏以旧契约名返回大成；DB 写入 `partner_id=8659059191`、结果 success、错误清空，health observer 恢复 ok。2026-07-22 追补：通用 `get_send_as_tags()` 纳入持久化历史用户名，使小世界天灾、题库、被动 inbox、元婴等复用 tag 的广播解析也能识别旧名；真实旧名天灾回放确认会归属当前吧唧身份并进入布道安抚。定向 `106 passed`，全量 `3237 passed, 405 subtests passed`。 |
| 进行中 | 2026-07-16 wxjerry/Rust 更新吸收 | 对照 wxjerry/Rust 最新线，只吸收本地真实缺口，不整包搬状态机，不扩大主动发送面。 | 持久化 P0/P1/P2 已完成：差量写、六小时双代 guard、结构变化即时备份、current/previous 校验恢复均上线；2026-07-29 08:25 首次自然双代轮换 current/previous 均 quick_check=ok、24 身份、pending=0。wxjerry 世界 Boss WebSocket 的 `/ws-ticket`、同源校验、ping/状态解码、重连和 HTTP 权威回退已由提交 `7cbc67df` 上线，依赖 `websockets 15.0.1`，全量 `3411 passed, 535 subtests passed`。最新 `origin/main=8ff9d90f` 仅精简侍妾/小世界日志，本地实现结构不同且无同类缺口，不机械搬运；本轮已吸收万兽谷驭灵行迹 Gate A/B 协议层，证据见 `docs/audit/spirit-beast-miniapp-gate-ab-20260729.md`，仍默认关闭、无 scheduler。剩余真实缺口为 Gate C 单身份行迹状态机、南宫阙实时 Boss 独立 Gate，以及账号目标群成员门禁等候选；继续 Lab-first。 |
| 已销号 | 2026-07-16 洞府深闭非幂等 HTTP 重试 | 洞府公共入口的深度闭关 `start/settle/force` 属状态变更 POST；传输结果未知时不得由 HTTP 层自动重放，应等待后续状态/总结证据校准。 | `model/features/cave_treasure_miniapp.py` 的深闭生产 flow 已显式使用 `backoff_sec=()`，与寻宝动作、天机阁命令的单次提交契约一致；新增 `test_deep_seclusion_action_flow_disables_http_retries`。验证：洞府/WebApp focused `132 passed, 15 subtests passed`，`py_compile`、`git diff --check` 通过；02:47 重启后 24 身份恢复成功、pending=0。真实闭环：Lpprceqei 03:15:39 单次 `settle` 回放总结并进入 `post_summary_wait`，03:18:11 单次 `start` 成功进入 running、下一轮 11:18、probe 清零；未见第二次 POST、429、重复总结或启动风暴，health/watchdog ok。 |
| 补充销号 | 2026-07-22 wxjerry/Rust 吸收项 P1 持久化差量写 | P1 只处理无变化保存的差量快照与 schema cache；不接管备份代次、备份节流或恢复控制。 | Lab `lab/persistence-gate-20260722` 提交 `26719083`，focused `105 passed`，全量 `3250 passed, 405 subtests passed`，py_compile/diff check 通过；审计见 `docs/audit/persistence-p1-gate-review-20260722.md`。生产显式备份 `data/state/chaogu_state.db.pre-persistence-p1-20260722193803`，integrity_check=ok；上线提交 `b99a817b`，19:39 重启后服务与 watchdog active、状态恢复成功、pending=0，重启后影子 `24873 saves / 0 telemetry error / full_scope_count=0`。P2 仍阻断，wxjerry/Rust 总项继续保留进行中。 |
| 已销号 | 2026-07-14 小世界高香火身份级静默 | 当前 `b9631461` 只静默了小世界模块内的刷新/维护链，不足以实现“高香火后不说话就不容易被偷”。目标改为身份级静默：当某身份小世界香火库存达到阈值后，该 `send_as_id` 的游戏群主动发送默认进入 `small_world_silence` 保护，不限于 `.小世界`。静默口径：MiniApp 正常可跑，但必须只走纯公共入口 HTTP/WebView，不发送游戏群入口命令；天机阁/MiniApp 总预算按官方口径 20000/日、90/分钟，预算优先留给 WA 和吧唧。静默期只走 MiniApp 和真正长 CD 游戏命令。默认阻断范围包括野外/裂缝/天星（观命/定命/推命/改命/查盘等均属高频/高风险）、斗法、合欢温养/双修温养、慕兰、问道、婉心/侍妾、钓鱼触发命令、观星台、天机试炼入口、洞府入口触发、宗门/点卯/闯塔、法宝抚摸/法宝养护、宠物/灵兽抚摸、储物袋/副本加入等会在游戏群冒泡的动作；允许范围包括被动解析、日志群播报、UI/本地状态更新、纯 MiniApp 公共入口 HTTP 且不需要游戏群发言的动作、长 CD 维护白名单（闭关/深度闭关启动或续轮、第二元神长 CD 训练/出窍链、元婴出窍/元婴闭关等），以及用户手动临时解除。长 CD 白名单只放真正启动/续轮动作，不放主动状态探测（如 `.查看闭关`、`.元婴状态`、`.第二元神` 查询），也不包含天星、温养、抚摸；发送状态未知时不自动补发，按日预算限次。护界先不纳入本轮；显灵只在已有真实祈愿证据且用户明确开启时作为可配置例外，禁止为了找祈愿而刷新。实现上优先做中心化 pre-send guard 或调度候选过滤，避免逐模块补漏；健康观察需新增“静默身份仍发生非白名单主动游戏群发送”的 read-only 告警。 | 2026-07-15 用户决定暂不采用身份级中心静默，故停止该扩展并销号；未上线中心 pre-send guard、调度过滤、重试接管或状态写入。保留小世界模块内部的可选保护，但新增身份级 UI 开关“高香火静默”，默认及现有身份迁移值均为关闭；只有用户显式打开某身份后，达到护界库存阈值才跳过小世界主动刷新/维护。只读审计工具继续保留，不参与运行控制。 |
| 已销号 | 2026-07-05 Telegram WebApp/MiniApp 统一层调研 | 钓鱼、世界 boss 等玩法迁移到小程序后，不能按普通命令模块硬刷。先找成熟开源仓库复核 WebApp/MiniApp 做法，沉淀通用架构：WebView initData 获取、敏感字段脱敏、按钮/URL 白名单、HTTP/WebView 适配分层、手动诊断优先、lab-first、默认关闭。禁止长期保存 `tgWebAppData/initData/query_id`，禁止绕过安全锁。世界 boss MiniApp 还要额外遵守账号边界：同一登录账号同一场只能选一个角色入场，后续自动入场候选必须按 `identity_account_map` 去重，最多 4 个登录账号。 | 已只读核对 Blum/Hamster/TimeFarm/MemeFi/NotPixel/Depin 等公开 MiniApp 自动化仓库，结论写入 `docs/lab_webapp_miniapp_research_20260705.md`。2026-07-05 lab 新增 `model/webapp_core.py` 通用安全摘要 helper，并接入 `model/app_message_log.py` 按钮日志：仅记录 host、start 参数脱敏摘要、敏感键名和玩法候选，不保存完整 URL/initData/hash/user；验证 `tests/test_webapp_core.py tests/test_log_entries.py tests/test_tiandao_judgement.py` 40 passed。2026-07-06 主控复核后将统一层随服务受控上线：钓鱼仅接 `fishing_enabled` 闸门后的 MiniApp 入口 handler，观星台/世界 boss 仅入口识别和 registry，不恢复旧文本链、不自动出手；验证 `.venv/bin/python -m pytest -q tests/test_retry_scheduler.py tests/test_webapp_core.py tests/test_miniapp_entry_probe.py tests/test_fishing_runtime.py` 107 passed，`compileall`、`py_compile`、`git diff --check` 通过；07:45 重启后 `xiuxian.service` active，pending=0，`global_enabled=1`，24 个身份的 `fishing/stargazer/guanxing/world_boss` 开关均为 0。2026-07-09 追加确认新版世界 boss 文案为按钮入场且“本轮废弃破幡、镇魂、护阵，强攻即为主要攻击”，主线已新增 MiniApp-only 开场识别、结算榜/保底收获解析，以及按登录账号去重的候选选择器；仍不自动入场，等 `qyz_*` 协议抓包后另行灰度。 |
| 已销号 | 2026-07-05 灵溪垂钓 MiniApp 纯 HTTP 方案评估 | 用户提供逆向方案：`.钓鱼` 获取 `fish_TOKEN` 后用 Telethon `RequestMainWebView` 取 `initData`，再按 `/api/miniapp/xianxia-fishing/start` 两次、`/finish`、`/result`、可选 `/next` 完成钓鱼；`fishingProof` 分数需自然随机，不能固定满分。后续必须先在 lab 只读/低风险验证 token、initData 生命周期、请求签名、端点 host 白名单、失败退避、日竿数和安全锁接入；默认关闭，不长期保存 `initData/tgWebAppData/query_id`，不独立绕过 WebApp 统一层。 | 2026-07-05 已入钓鱼待办。2026-07-06 主控确认从 lab 候选进入受控上线：`model/webapp_core.py`、`model/features/fishing_miniapp.py`、`model/features/fishing_runtime.py`、`model/app.py` 已接生产入口 handler，但仍受 `fishing_enabled` 总开关控制；不保存 raw `initData/tgWebAppData/query_id/hash/user`；补账号归属解析，避免接管别人的 MiniApp 入口。09:30 吧唧 canary 已跑通 5/5：入口只发 `.钓鱼 青溪浅滩 凡饵`，MiniApp 接管完成，未再发送 `.钓鱼状态/.提竿`。后续修正连续竿策略：MiniApp 完成后优先调用页面“再来一杆”对应的 `/next` 获取新 token，在同一 WebApp/HTTP 流程内连钓；若 `/next` 无 token 或失败，只落账当前竿并进入 MiniApp 退避，不回退群里重发第二条 `.钓鱼`。12:12 追补：移除单入口固定 5 竿 cap，改为按鱼篓/本地/MiniApp daily used-limit-remaining 计算剩余竿数，MiniApp 若返回 daily/quota/remaining 上限会自动校准本地 `fishing_daily_limit`；本地保护上限放宽到 99，避免未来游戏调整次数后被 20 截断。15:52 追补：生产钓鱼 MiniApp flow 已接入脱敏 capture sink，后续真实 HTTP 调用会写入 `data/state/miniapp_capture/fishing-YYYY-MM-DD.jsonl`，只保存 endpoint、payload/response shape、安全摘要、状态码和耗时，不保存 raw token/initData/hash/user，用于校准鱼获字段解析。验证：`.venv/bin/python -m pytest -q tests/test_fishing.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_fishing_app_routing.py tests/test_webapp_core.py tests/test_miniapp_entry_probe.py` 196 passed；12:10 后追补验证 `.venv/bin/python -m pytest -q tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_fishing.py tests/test_webapp_core.py tests/test_miniapp_entry_probe.py` 205 passed；15:52 验证 `.venv/bin/python -m pytest -q tests/test_webapp_core.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_miniapp_entry_probe.py tests/test_trial_runtime.py tests/test_ui_dual_track.py` 143 passed，`py_compile`、定向 `git diff --check` 通过；重启后 `xiuxian.service` active，UI smoke 16 checks ok，defensive preflight 为 watch/pending empty。 |
| 已销号 | 2026-07-06 MiniApp 成果日志物资口径 | 所有 MiniApp 的日志群“成果/收获”只播游戏内物资收益和可理解收益：鱼获、材料、灵石、修为/经验、伴生机缘等；禁止把 `score/session/ready/rounds/quality_bonus` 等技术字段当成果。钓鱼先落地，观星台、天机试炼、洞府寻宝后续上线前按同一口径补测试和日志。 | 2026-07-06 钓鱼侧已整改并上线验证：MiniApp 物资摘要改为鱼获/奖励/收益白名单，解析不到物资时只写“未解析到新增物资”且不发收获日志；新增回归覆盖“只有 bonusLoot/rewards 也播物资”和“只有 score/session 等技术字段不播收获”。天机试炼手动灰度结果已补收益/奖励白名单，汇总 `expGain/traceGain/rewards/bonusLoot`，不播 score/session。观星台当前按 `item_deltas` 播材料；洞府寻宝手动灰度结果已补收益/奖励白名单和回归，状态/step/session/score 不作为成果，真实回包字段待 capture 校准。2026-07-08 继续整改 `tools/miniapp_daily_report.py`：天机试炼日结只用服务端 `event_id` 去重，避免同一入口连续多轮因 `source` 相同被合并；洞府寻宝每条 `action:settle` 视为一局，不再用 `source/score` 合并；新增识别 `traceGain/experienceGain/cultivationGain/xiuweiGain/lingshiGain/stoneGain/contributionGain` 和 `{物品: 数量}` 型 `item_deltas`，观星台收集精华也可从映射入账。验证 `.venv/bin/python -m pytest -q tests/test_miniapp_daily_report.py tests/test_trial_runtime.py tests/test_cave_treasure_runtime.py tests/test_stargazer.py tests/test_webapp_core.py tests/test_miniapp_entry_probe.py` 125 passed / 5 subtests，`py_compile tools/miniapp_daily_report.py`、`git diff --check` 通过；提交 `ed3bc9b Tighten MiniApp reward daily report` 已推送。2026-07-08 08:38 追补：日结不再播只有动作/模式/跑分且无物资的观星台、灵树行；零收益不再进入 Counter，避免 `收益:无` 这种空成果。验证 `tests/test_miniapp_daily_report.py` 3 passed，MiniApp 扩展回归 `tests/test_miniapp_daily_report.py tests/test_trial_runtime.py tests/test_cave_treasure_runtime.py tests/test_stargazer.py tests/test_webapp_core.py tests/test_miniapp_entry_probe.py` 127 passed / 5 subtests；7/7、7/8 真实 capture 补播文本已确认只剩鱼获、材料、灵石、经验/天机残痕、洞府贡献等物资收益。2026-07-08 08:46 追补灵树即时结果日志：`model/features/tree_runtime.py` 不再播分数/目标/best，只保留结算状态和“未解析到新增物资”；次数用完状态只报用量，不报 best 分。新增 `tests/test_tree_runtime.py` 覆盖。2026-07-18 负债复核：7月17日真实结果已覆盖灵树、观星台、天机试炼和洞府寻宝；即时与日结仅输出天机残痕、贡献、妖丹、药草、种子、灵石等游戏收益，未把 score、session、rounds 或 ready 当成果。当前全量 3211 passed、396 subtests passed，生产 health 正常，验收完成。 |
| 已销号 | 2026-07-07 钓鱼 MiniApp 全体日结日志 | 用户纠正“钓鱼日结”口径：不是单个角色钓完就发一条日结，而是当天所有开启灵溪垂钓的角色完成后，按角色汇总鱼获、伴生机缘、经验等游戏物资收益，再向日志群推送一条全体日结；伴生机缘即时提醒保持不变。 | 2026-07-07 已改为扫描所有启用灵溪垂钓的身份：全部达到各自当日上限、没有待回复/待动作/非 idle 后续、且至少有可解析游戏收获时，才发送一条“灵溪垂钓日结｜全体”；发送成功后给参与身份同步标记当天已播，伴生机缘即时提醒逻辑不变。2026-07-15 真实日结暴露频道冻结身份被旧 `get_identity_enabled()` 提前过滤，首报错误缩成 `15/15竿｜角色3`；提交 `486d0e8f` 改为复用公共入口身份可用性，人工停用仍排除，无鱼竿和旧 `daily_limit` 终态不阻塞，零收获角色不进入正文。验证：钓鱼/MiniApp 定向 `154 passed, 5 subtests`，全量 `3069 passed, 396 subtests`；重启后自动补正为 `35/35竿｜角色7`，23 个公共入口钓鱼身份全部标记当天已播，pending=0、健康检查 ok、无重复播报。 |
| 已销号 | 2026-07-07 MiniApp 低完成度并行交接 | 用户要求把完成度低的 MiniApp 工作交接出去并行做。交接范围限定为 lab/code/test/docs，不允许辅助方重启服务、打开开关、执行 live probe、发送游戏命令、push 或读写生产 DB。 | 辅助交接已合并为 `docs/miniapp_aux_fix_handoff_20260708.md`，主线已复核并处理阻断项：天机试炼 dict key-only 点位/节点保留 id，planarity 无有效节点 fail-closed；MiniApp 入口按钮解析、非幂等 POST 禁重试、tree 分数上限和 UI 分数配置均已进入主线。验证随提交 `c0505cd Stabilize bot health and miniapp flows` 完成：full pytest 通过，`py_compile`、`node --check`、`git diff --check` 通过；服务已重启上线。剩余“真实 live 跑全账号/生产灰度”分别归入天机试炼、洞府寻宝、观星台、MiniApp 成果日志等独立事项，不再作为交接待办。 |
| 已销号 | 2026-07-06 天机试炼 MiniApp 纯 HTTP lab 接入 | 用户提供逆向方案：`.天机试炼` 获取 `trial_TOKEN` 后用 Telethon `RequestMainWebView` 取 `initData`，再按 `/api/miniapp/xianxia-trial/start` 读取 challenge/sequence/trapIds，生成 `trialProof` 后 `/finish`，可选 `/next` 连续试炼。先按 MiniApp 统一层接入 adapter、flow plan、请求构造、proof 求解和 mock 测试；默认 `manual_only/default_enabled`，不接生产 scheduler，不保存 raw `initData/tgWebAppData/query_id/hash/user`，不绕过全局锁/安全锁/看门狗。 | 2026-07-06 已入账并完成 lab 候选接入：新增 `model/features/trial_miniapp.py`，已接 `miniapp_registry`、入口诊断、token 脱敏与文档；保持 `manual_only/default_enabled=False`，不接生产 scheduler，不会自动发送 `.天机试炼`。2026-07-06 15:35 追补：新增 `model/features/trial_runtime.py` 手动灰度执行层，UI `/api/miniapp-manual-run` 仅允许当前身份一次性授权 `trial`，发送 `.天机试炼` 后才接管对应 MiniApp 入口；不接定时 scheduler、不裸接未授权试炼回包。2026-07-06 15:58 追补：手动灰跑接入脱敏 capture sink，后续真实试炼会写入 `data/state/miniapp_capture/trial-YYYY-MM-DD.jsonl`，只保留 endpoint/payload/response shape、安全摘要、状态码和耗时。验证：`node --check model/web/static/js/miniapp_ui.js`、`.venv/bin/python -m pytest -q tests/test_webapp_core.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_miniapp_entry_probe.py tests/test_trial_runtime.py tests/test_ui_dual_track.py tests/test_stargazer.py` 165 passed；15:58 追补验证 `.venv/bin/python -m pytest -q tests/test_webapp_core.py tests/test_trial_runtime.py tests/test_miniapp_entry_probe.py tests/test_ui_dual_track.py` 66 passed；`py_compile` 与定向 `git diff --check` 通过。2026-07-08 维护期复核发现每日自动批量在 `global_enabled=0` 时可能先写当天批次已启动，再被发送层拦截，导致恢复后当天漏跑；修复 `model/ui.py::run_miniapp_daily_scheduler`：全局暂停时返回 `global_disabled`，不取身份、不创建 batch、不写 done。补 `tests/test_miniapp_entry_probe.py::test_miniapp_daily_scheduler_waits_when_global_disabled`；验证 `.venv/bin/python -m pytest -q tests/test_miniapp_entry_probe.py tests/test_ui_http_smoke.py tests/test_ui_dual_track.py` 35 passed，扩展 `.venv/bin/python -m pytest -q tests/test_miniapp_entry_probe.py tests/test_miniapp_daily_report.py tests/test_trial_runtime.py tests/test_cave_treasure_runtime.py tests/test_miniapp_protocol_flows.py tests/test_webapp_core.py tests/test_app_scheduler_contract.py` 175 passed / 5 subtests，`py_compile model/ui.py`、`git diff --check` 通过；提交 `7667606 Hold MiniApp daily trial while paused` 已推送并重启 `xiuxian.service/xiuxian-listener.service`，四服务 active，`global_enabled=0` 与 pending `.登天阶` 保持。2026-07-18 负债复核：7月17日生产公共入口串行批次 12/12 身份成功，每身份结算3次，共36次，汇总天机残痕+516；无群命令补发、无并发风暴、收益口径正确，验收完成。 |
| 进行中 | 2026-07-22 天机阁分层迁移第一项 `.天阶状态` | 只读命令进入严格白名单，禁止 HTTP 重试；公共入口手动 `tianti_status` 已将 `actionResult.rawMessage` 接入既有天阶解析器，更新状态/冷却/问心计划但不触发登阶；Lab 已通过 115 passed / 5 subtests。自动调度替换和单身份生产观察仍待后续 Gate，不批量开启。 |
| 已销号 | 2026-07-06 洞府总控/寻宝 WebApp/MiniApp 接入 | `.洞府` 群命令入口已废弃，洞府系自动化只允许走公共入口 `df_*`；频道身份也可通过同一公共入口切换/选择身份。用户描述新玩法：公共入口进入洞府后切换到“寻宝”，面板里神识 `8/8` 表示单局剩余出手次数/总可用次数，游戏 `0/3` 表示今日已玩局数/总可用局数；`0/8` 是单局用尽，`3/3` 是今日结束。自动化需先按 MiniApp/WebApp 统一层 lab-first 采集协议，不写死次数；当前样本“入府后点小人 7 次”只能作为页面样本，正式实现必须优先读取页面/接口真实 ratio/remaining/limit。流程：切换寻宝后点击“入府”，对下方小人按剩余神识次数探索；若命中宝物则按页面状态“再来一次”后“见好就收”结算，未命中也“见好就收”结算并循环下一局。若接口直接给答案则用答案；拿不到答案时先读中间文案提示，无提示才随机。洞府总面板还包含闭关/深度闭关状态、天机阁元婴、小世界、天机试炼、观星台/灵圃等入口：闭关和元婴要优先公共入口，走不通才保守退让命令；外部入口只保存脱敏摘要并交给对应 MiniApp 模块处理。公共入口必须支持多 URL 候选和健康切换，避免绑定单个天尊 shard 后对应 bot/入口失效导致整条链坏掉；入口健康失败可切备用，业务态次数用尽/CD/今日已完成不得切备用重跑。默认关闭、手动/lab 先行，不保存 raw `initData/tgWebAppData/query_id/hash/user`，不绕过全局锁/安全锁。钓鱼同理：每日竿数不得写死 5/20，必须以鱼篓或 MiniApp used/limit/remaining 为准，钓到页面/接口上限为止。 | 2026-07-06 已按用户要求入账；已找到真实 `.洞府` 回包入口样本，按钮 start 参数为 `df_*`。已完成 lab-only 初版：新增 `model/features/cave_treasure_miniapp.py`，接入 `miniapp_registry`，补 `df_*` 脱敏和洞府入口推断；状态解析区分“神识剩余/总数”和“游戏已用/总数”，决策覆盖切换寻宝、入府、按提示/随机点小人、命中后再来一次/结算、未命中耗尽后先结算；不接生产 scheduler、不加 UI 发令入口。2026-07-06 14:32 追补：修复提示文案里“宝光”被误判为已命中，`hint/tips` 只用于选目标，命中/再来一次必须来自结果/状态或显式字段；新增回归覆盖“命中后先再来一次，再见好就收”。2026-07-06 14:45 追补：曾加入 `.洞府` 入口诊断白名单；2026-07-14 已按公共入口唯一口径废弃该群命令诊断路径，后续洞府系只从配置的公共入口候选池启动。2026-07-06 14:52 追补：新增 `/api/miniapp-status` 只读快照，展示 adapters、flow plans、entry probe commands 和默认安全策略，不发送命令、不输出 raw token/initData。2026-07-06 15:22 追补：新增 MiniApp 二级 UI 弹窗。2026-07-06 20:55 追补：新增手动灰度运行层 `model/features/cave_treasure_runtime.py`；2026-07-14 起 UI 手动和后台批次不再发送 `.洞府`，而是使用公共入口候选 URL，真实调用仍写入 `data/state/miniapp_capture/cave_treasure-YYYY-MM-DD.jsonl`，只保存 endpoint、payload/response shape、安全摘要、状态码和耗时；结果日志按物资口径播收益/奖励，不播 session/score/step。2026-07-06 追补：已按真实前端协议校准为 `/api/miniapp/xianxia-dwelling/start|hunt|hunt/reveal|hunt/settle`，payload 包含 `sessionId` 与 0-based `index`；验证覆盖洞府 runtime、入口诊断与 WebApp core。2026-07-08 继续复核决策边界：按钮/操作文案里的“见好就收/再来一次”不再单独视为命中宝物，只有显式 found/huntResult 或“发现宝/命中宝/主宝/秘宝/宝物到手”等结果文案才触发结算，避免在仍有神识次数时提前 settle。补 `tests/test_miniapp_protocol_flows.py::test_cave_treasure_action_button_text_does_not_mean_treasure_found`；验证 `.venv/bin/python -m pytest -q tests/test_miniapp_protocol_flows.py tests/test_cave_treasure_runtime.py` 31 passed，扩展 `.venv/bin/python -m pytest -q tests/test_miniapp_protocol_flows.py tests/test_cave_treasure_runtime.py tests/test_miniapp_entry_probe.py tests/test_webapp_core.py tests/test_miniapp_daily_report.py` 121 passed / 5 subtests，`py_compile model/features/cave_treasure_miniapp.py`、`git diff --check` 通过；提交 `a3c40c9 Avoid cave treasure button-text settle` 已推送并重启服务，四服务 active，`global_enabled=0` 与 pending `.登天阶` 保持。2026-07-10 追补：`/start` 总面板解析已覆盖 `dwelling.meditation.standardCultivation/deepSeclusion` 和 `account.externalApps` 的 `farm/trial` 入口摘要；WA 寻宝 3/3 lab 实测跑通，capture 已清洗为只保留 shape/key/digest。2026-07-18 负债复核：公共入口多候选已上线；7月17日4个登录账号寻宝均按动态上限完成3/3并解析贡献、妖丹、药草、种子和灵石，深闭 settle/start、天机阁元婴、观星台与钓鱼也持续闭环。后续口径以第59行双轨纠偏为准，不再沿用本行早期“群命令入口废弃”的旧描述。 |
| 已销号 | 2026-07-05 观星台 MiniApp 迁移 | 用户确认观星台也更新成 MiniApp；旧 `.观星台`/牵引星辰/安抚星辰/收集精华链路不得继续按普通文本模块硬刷。后续与钓鱼 MiniApp 一起接入 WebApp 统一层：入口按钮识别、start 参数摘要、lab-first 复核真实协议、host 白名单、initData 生命周期、失败退避、UI 可控和审计日志；默认关闭，不保存敏感 initData，不绕过安全锁。 | 2026-07-05 已先全量关闭旧观星台相关开关：`stargazer_enabled=0`、`guanxing_enabled=0`、`guanxing_monitor_enabled=0`；DB 备份 `data/state/chaogu_state.before-disable-stargazer-miniapp-20260705-230419.db`。2026-07-06 已对照 wxjerry 线补齐 farm 协议执行层：`.观星台` 只作为入口，拿到 `farm_*` 按钮后用 RequestMainWebView 获取短 TTL initData，再走 `/api/miniapp/xianxia-sect-farm/start|action`，按状态自动 soothe/collect/pull/wait，奖励以脱敏摘要和背包 delta 入账；UI `/api/miniapp-manual-run` 增加 `stargazer` 单次授权，生产 scheduler 仍默认关闭，不恢复旧 `.牵引星辰/.安抚星辰/.收集星辰精华` 文本链。真实 HTTP 调用会写入 `data/state/miniapp_capture/stargazer-YYYY-MM-DD.jsonl`，只保留 endpoint/payload/response shape、安全摘要、状态码和耗时。验证：`.venv/bin/python -m pytest -q tests/test_webapp_core.py tests/test_stargazer.py tests/test_miniapp_entry_probe.py tests/test_ui_dual_track.py` 87 passed；`.venv/bin/python -m pytest -q tests/test_webapp_core.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_trial_runtime.py tests/test_miniapp_entry_probe.py tests/test_ui_dual_track.py` 145 passed；`py_compile`、`node --check model/web/static/js/miniapp_ui.js`、定向 `git diff --check` 通过。 |
| 已销号 | 2026-07-05 全量审计报告复核整改 | 审计报告路径：`/root/xiuxian-main-full-audit-20260703.md`。先复核每条 finding 是否仍适用于当前代码，再按风险排序整改；每项整改必须有 focused 测试、静态检查、上线观察和销号记录。 | 高风险 finding 已逐项复核并按真实适用性修复；尾项 closeout 见 `docs/audit/full-audit-tail-closeout-20260722.md`。listener sidecar 已明确要求独立授权 session，生产无授权时主动降级为主 runtime listener；心跳改为 15 秒批量落盘并清理崩溃临时文件。全局发送锁经有界等待、RPC timeout 释放和取消语义复核后保留，不按原报告冒险拆锁。delayed action 永久失败行已在业务 handler 收到结果后删除，恢复仅接受 pending。此前已完成 setup mode、消息日志 claim 回滚、天星 await 覆盖、慕兰代次、被动账本热路径、UI 请求边界和 account-unbound 等整改；该审计批次完成，后续新异常进入独立事项。 |
| 已销号 | 2026-07-22 运行健康 UI 实时态补齐 | 健康卡不能只展示 `health_observer/latest.json` 的滞后结论；需同时显示当前 Bot health、全局启停及暂停来源、listener 状态，并区分任务队列 pending 与模块内部 pending，避免“快照 100 分但实际已暂停”或把两类 pending 混为一谈。 | `model/ui.py` 的健康快照合并实时 `get_bot_health_snapshot()`、全局控制、listener 摘要及两类 pending；前端按实时 Bot/暂停状态提升告警等级，并展示游戏 Bot、全局、Listener 三个状态和四项统计。生产读取确认 `global_enabled=true`、Bot `healthy`、任务/模块 pending 均为 0，listener `stopped` 会明确显示而不再藏在旧快照里。验证：`tests/test_ui_dual_track.py` 12 passed，UI smoke 16 checks，JS 语法、compileall、全量 `3237 passed, 405 subtests passed`。 |
| 已销号 | 2026-07-08 天尊健康暂停与自动恢复 | 天尊/游戏 Bot 宕机时，脚本必须在确认全局静默后自动暂停普通发送，保留 pending 和状态证据，不清空队列；恢复时不能凭单条普通消息立即放开，必须先进入探测确认，再由探测后的 Bot 回复自动恢复全局。阈值采用 300 秒静默暂停，120 秒过短、600 秒偏慢。验收：宕机窗口不继续风暴；暂停后 pending 可用于回捞/校准；Bot 恢复后 probe -> recover 自动恢复全局；普通手动暂停不被普通 Bot 发言误恢复；focused 测试覆盖暂停保留 pending、探测恢复、手动暂停不误恢复。 | 2026-07-08 已上线：`BOT_SILENCE_TIMEOUT_SEC=300`，天尊健康暂停不再清空 pending，`global_pause_source=bot_health_monitor` 持久化，恢复链路改为 bot 活动后 probe、probe 回复后 recover，手动暂停不会被普通 bot 发言误恢复；提交 `c0505cd Stabilize bot health and miniapp flows` 已推送。2026-07-09 至 07-14 真实静默窗口多次完成暂停→探测→确认回复→恢复；07-14 重启竞态曾出现 paused/probing 反复切换，随后由 `e1d5466b` 严格绑定探测回包、`06f03b7d` 稳定探测状态、`f12cc738` 收敛恢复/关停流程并补回归。验证：`tests/test_app_scheduler_contract.py` 天尊健康恢复/持久化暂停/手动暂停用例通过，全量 `3213 passed, 396 subtests passed`，compileall、diff check 通过；07-18 重启后四服务 active，`global_enabled=1`、pending=0、health observer `100/100`，未见恢复风暴。验收完成。 |
| 已销号 | 2026-07-09 斗法 lab 候选主线审计上线 | 候选代码在 `model/features/duel.py`，测试在 `tests/test_duel.py`，交接文档 `docs/duel_aux_handoff_20260708.md`。主线必须先复核真实日志口径、非战报终局计数、18-32/30-55 分钟冷却、目标池错峰和默认关闭边界；focused/full 相关测试、`py_compile`、`git diff --check` 通过后才可上线。上线时保持 `duel_enabled=0` 默认关闭，仅对明确授权身份小批量灰度，观察无异常后再扩大。 | 2026-07-15 已完成主线审计、上线和真实灰度。WA 批次严格执行 5/5：每场前确认 `.推命 斗法`，战斗时仅装备玄铁剑，结束后恢复青竹蜂云剑（神雷版）、乾蓝冰焰、青竹蜂云剑（金雷竹·庚金相）、元合五极山、玄天斩灵剑；日志 17:19:40 进入恢复、17:21:34 确认停止并恢复配装。相关修复 `23a4ab9c`、`1f2d8072` 已上线，全量回归 `3088 passed, 396 subtests passed`。目标共享 CD 的后续增强继续由独立事项 `2026-07-13 斗法目标被挑战 CD 全局校准` 跟踪。 |
| 已销号 | 2026-07-13 斗法目标被挑战 CD 全局校准 | 同一斗法目标必须在所有身份间共享至少 10 分钟互斥，且不能只依赖本方最后一次发送。真实出现 `@ccahen` 在本地锁已过后仍回复“元神尚未平复，5分钟内无法再次斗法”，说明目标可能被其他玩家占用。需复核：成功战报从发送时还是结算时起锁、目标冷却回复如何延长全局锁、迟到回复/重启恢复是否保留锁、候选目标池是否能在目标占用时轮换，以及日志/健康观察不得把正常目标 CD 记成业务错误。验收需用真实时间线回放多身份共享锁，保证任何已知目标 CD 窗口内不再发送；外部占用无法预知时首个 CD 回包只校准锁且不计次数、不立即重撞。 | 2026-07-13 入账。第一层止损：正常战报锁共享目标至少 10 分钟；点名目标 CD 时追加 5 分钟争用缓冲。真实时间线又定位到简版终局清锁缺口：12:45 `growrdick` “侥幸逃脱”后共享锁被清，12:46 `imcanonical_ai` 重撞；现已将真实交战简版终局纳入目标消费证据。进一步按用户指出补全外部占用采集：群内任何完整斗法战报只要包含当前配置的候选目标，即使参战双方都不是本方身份，也会被动刷新该目标共享 10 分钟锁；只更新目标 CD，不计本方次数、不碰 pending、不发送命令。2026-07-18 复核真实时间线发现最后缺口：WA 05:22 手工斗法战报仍返回旧名 `@jfdffdddd`，配置目标已变更为 `@jfdffdddd1`，被动观察未匹配历史别名，导致 05:30 脚本提前出手。`model/features/duel.py` 已将目标观察、广播战报和点名 CD 统一改为数字身份维护的当前名+历史别名集合匹配；新增真实旧名时间线回归，确认手工战报会刷新当前目标键并阻断 8 分钟后的自动发送。验证：focused `173 passed, 4 subtests passed`，full `3213 passed, 396 subtests passed`，compileall、diff check 通过；提交 `4a49d619` 已推送并上线，重启后 health 100、pending=0、watchdog 正常。 |
| 已销号 | 2026-07-08 慕兰午夜批量错峰修复 | 慕兰日常不能在 00 点后因启动恢复或完成冷却集中释放 `.搜集军报/.支援慕兰`。恢复和次日 cooldown 必须稳定拆到 01:00-04:00、05:00-08:00 两批；已写入旧 00:xx due 的状态在恢复调度时必须先重排到批次窗口，不得立即发送。 | 根因：`schedule_mulan_initial_check()` 使用 5-25 分钟恢复窗口，`_daily_done_delay_sec()` 使用次日 00:10+抖动，导致 23 个慕兰身份次日集中在 00:11-00:13；全局暂停时普通调度跳过，旧 due 还需恢复后自愈。修复：`model/features/mulan.py` 新增两批窗口计算、次日批次 cooldown、旧 00:xx cooldown 自愈；`tests/test_mulan.py` 覆盖午夜前启动、错过第一批、完成后次日批次、旧 00:xx 未过点和已过点均不发送。验证：`.venv/bin/python -m pytest -q tests/test_mulan.py tests/test_startup_recovery_guards.py tests/test_app_scheduler_contract.py` 128 passed / 12 subtests，`.venv/bin/python -m pytest -q` 2631 passed / 377 subtests，`py_compile`、`git diff --check` 通过；已重启 `xiuxian.service`/`xiuxian-listener.service`，四服务 active，`safety_watchdog --once --dry-run` 为 ok；提交 `bf1cee2 Stagger Mulan daily recovery windows` 已推送。 |

补充进行记录（2026-07-05 01:20）：复核审计报告 high 项时确认被动账本热路径与 setup mode 鉴权仍适用于当前代码；已修复 `model/features/passive_inbox.py`，被动统计落盘只保存轻量计数，不再每条事件重建 contract-gap 汇总；已修复 `model/ui.py`，初始化免鉴权 fake session 仅允许本机 loopback 访问，公网首装窗口不再可抢绑账号，同时保留本机首装能力。验证：`.venv/bin/python -m pytest -q tests/test_audit_regressions.py tests/test_ui_auth.py tests/test_ui_http_smoke.py tests/test_message_contract.py` 36 passed；`py_compile`、`git diff --check`、重启后 `health_observer`、`safety_watchdog`、`defensive_preflight` 通过。下一步继续复核全局锁、FloodWait、send_as、pending retry 等报告项，不照单盲改。

补充进行记录（2026-07-05 01:24）：复核 pending retry 审计项确认仍成立：`run_retry_scheduler` 在 retry 发送返回 `None` 时会删除旧 pending，导致排队/发送超时、FloodWait、账号暂不可用等瞬时故障后失去回复追踪。已修复 `model/runtime.py`：retry 未实际发出时保留原 pending、延后下一轮并记录 `retry_send_blocked_*` 审计字段；只有新消息真实创建后才替换旧 pending。验证：`.venv/bin/python -m pytest -q tests/test_retry_scheduler.py tests/test_safety_watchdog.py tests/test_app_scheduler_contract.py` 153 passed；`py_compile`、`git diff --check`、重启后 `health_observer`、`safety_watchdog`、`defensive_preflight` 通过。

补充进行记录（2026-07-05 01:28）：复核 FloodWait 审计项确认当前代码只标记 bot suspect，未按 `FloodWaitError.seconds` 做硬退避，且 P0 仍可能继续撞 Telegram 限流。已修复为账号级 FloodWait 退避：触发 FloodWait 的物理账号在 seconds 窗口内阻断所有游戏发送优先级（含 P0/验证），其他账号不受牵连；发送前和拿到发送槽后各检查一次，阻断有 `flood_wait_backoff` 证据与限频审计日志。验证：`.venv/bin/python -m pytest -q tests/test_retry_scheduler.py tests/test_dungeon_quiet.py tests/test_safety_watchdog.py tests/test_app_scheduler_contract.py` 167 passed；`py_compile`、`git diff --check`、重启后 `health_observer`、`safety_watchdog`、`defensive_preflight` 通过。
| 已销号 | 2026-06-30 紧急：侍妾交换/赠予指数级异常 | 用户反馈“几何级数的侍妾交换出现问题”。必须按结构修复：统计 2026-06-29/30 真实日志里 `.赠予侍妾`、`.我的侍妾`、`.天机代卜`、`.入梦寻图`、`.共历心劫`、`.侍妾远航` 的同身份短窗重复；核对侍妾 scheduler、phaseful cleanup、超时恢复、gift/status/bag/send 链路是否存在多入口同时推进；补全“同身份同一侍妾模块同一时刻只能有一个 pending/active 动作”的单飞闸和状态恢复测试，不能靠看门狗兜底。 | 2026-07-18 结构化复核完成：6月29/30原始 `data/messages` 尚未建立，不能伪称完成缺失日志的逐条统计；保留 journald 仅见旧 `.入梦寻图` 归位补发和 6月30 两条发送超时。当前侍妾调度由 `_CONCUBINE_SCHEDULER_LOCK` 单飞，所有 status/greet/gift/dream/tianji/heart/voyage 动作共用一个 `concubine_phase`，gift 的 status/bag/send 三阶段分别持久化消息 ID，超时先按消息日志回捞再保守收口。7月3日至18日结构化 `sent` 仅有两次脚本 `.赠予侍妾`：7月3日 180、7月12日 70，均在1秒内收到对应成功回复，无同身份同日重复；7月17/18群内其他 `.赠送`/`.赠予侍妾` 均为手工或外部身份，未冒充脚本验收。新增回归固定“并发调度只启动一条 gift 链”和“重启清理 gift pending 后保留当日 attempt 闸、不再重发”；侍妾定向 140 passed。生产24身份当前全部 `concubine_phase=idle`、gift pending/message ID 均为0。 |
| 已销号 | 2026-06-30 紧急：天星宗全链路错误重审 | 用户反馈“天星宗全都错误了”。必须重新审计 `观命/定命/推命/改命/天机盘/炼制攒点/探索消费/消劫` 全链路：清理 stale `released_routes`，禁止在 `current_prediction/current_change` 与下游动作冲突时放行；修复 `.天机盘` 频繁校准、安全锁 guard 不关闭、WA 炼制触发 `推命落空/逆命劫 +1`、图图残留 `改命 闭关` 等真实状态污染；补真实日志回放和路由 preflight 测试。 | 2026-07-18 全链路复核完成。当前实现以时间线 plan/step 和真实 reply/edit 确认推进，路由结果会消费对应推命并清理 downstream release；冲突推命、冲突改命、stale release、send guard、资源不足、面板校准和迟到回复均有回归。真实证据：7月17日五个天星身份各一次观命/定命；WA 16:00 `.消劫` 成功清除1层；7月18日 05:54、06:03 两次“推命炼制→炼制最终编辑”均命中并各得天机+1；18:22/18:23 推命与改命探索确认后，18:32 仅发送一次深入野外，18:32:50 最终编辑命中并得天机+1，随后 DB `current_prediction=''`、`prediction_consumed_route=探索`、`released_routes={}`，改命探索仍有效，下一次野外/裂缝均回到重新准备。该过程发生在 `deep_retreat_phase=running`，未被深闭阻断。图图当前 `current_change=探索`，不再残留闭关改命；五个天星身份 `calamity_count=0`。7月17/18脚本未主动发送 `.天机盘`，无面板校准风暴。定向 `tests/test_tianxing.py tests/test_wild_training.py tests/test_explore_rift.py tests/test_defensive_preflight.py` 397 passed / 14 subtests；全量最近为 3225 passed / 396 subtests。其余四个频道身份当前因 send-as 关闭被合法冻结，不作为发送缺失。 |
| 已销号 | 2026-07-05 新天星观命后不定命/重复观命 | 用户反馈“新的天星疯狂观命不定”。必须复盘新入天星号真实日志，尤其 `xuruode6` 12:03/12:07/12:14 附近 `.观命` 与回包；修复观命回包已存在但本地状态未确认、action_guard 未闭合、`available_stars` 已有但 `fixed_star` 为空时不触发 `.定命` 的结构问题。验收：同日观命结果可回捞并写入；未定命时按 `star_priority` 只发一次 `.定命 <命星>`；发送失败/回包慢先回捞再校准，不进入观命风暴；补真实日志回放和天星默认配置测试。 | 2026-07-18 真实窗口验收完成。7月17日五个天星身份均恰好发送一次 `.观命` 和一次 `.定命 太阴`；`xuruode6` 00:01:03 观命、00:01:05 收到太阴/贪狼/紫微、00:01:48 定命、00:01:49 成功确认，未重复观命。五号 DB 的 `available_stars_day/fixed_star_day` 均为 2026-07-17、`fixed_star=太阴`。7月18日 WA 再次恰好各一次并成功；其余四个频道身份因 `channel_send_as_health=closed` 被自动冻结且 `identities.enabled=0`，没有发送属于合法停用，不是观命状态机卡住。现有回归覆盖观命日志回捞、每日定命、发送失败短退避、pending set-star 回复回捞和 action guard 收口；`tests/test_tianxing.py` 为 241 passed / 9 subtests。 |

补充进行记录（2026-07-06 00:17）：按用户要求取消天星炼制攒点的固定时段限制，保留世界 boss 与探索消费窗口避让；同时修复到期天星扫描每轮只处理前 2 个身份导致低天机号被饿死的问题，扫描排序改为时间线/恢复优先、炼制补点优先、同级低天机优先。验证：`.venv/bin/python -m pytest -q tests/test_app_scheduler_contract.py tests/test_tianxing.py tests/test_wild_training.py tests/test_defensive_preflight.py` 347 passed / 4 subtests passed；`py_compile`、`git diff --check` 通过；00:14 重启后 `xuruode6`、`xueuode5` 优先进入 `.推命 炼制`，00:16 后 DB 确认 `craft_result=炼制结束 玄铁剑`，天机分别 `3->4`、`5->6`，defensive preflight 为 watch、pending=0。继续观察 00:59 `xuruode6` 探缝和 01:02 起连续野外窗口。
| 已销号 | 2026-07-05 UI 模块开关打不开/保存失败 | 用户反馈 UI 上新来的两个天星开关打不开，且此前多次出现设置保存后被刷新覆盖、转移/赠送选择缺账号等问题。必须按公共 UI 保存链路排查：前端控件名、PATCH/POST payload、后端布尔字段白名单、身份切换 rerender、轮询刷新覆盖本地编辑、保存失败提示。验收：天星等模块开关可保存并即时回显；刷新后 DB 与 UI 一致；保存中不被自动刷新覆盖；补 UI/API focused 测试。 | 2026-07-08 根因确认：`ui_write_guard.js` 只挡 POST 进行中的静默刷新，用户正在设置弹窗/输入框编辑但未保存时，定时 `refreshState({silent:true})` 仍可能 `applySnapshot -> renderAll()` 覆盖本地编辑。修复 `model/web/static/js/ui_write_guard.js`：静默刷新遇到输入焦点、未保存编辑或打开设置弹窗时跳过；保存/手动快照回写后清理编辑态，不影响正常保存。补 `tests/test_ui_dual_track.py::test_ui_write_guard_blocks_stale_silent_refresh_snapshots`；验证 `node --check model/web/static/js/ui_write_guard.js`、`.venv/bin/python -m pytest -q tests/test_ui_http_smoke.py tests/test_ui_dual_track.py` 11 passed、`git diff --check`、`safety_watchdog --once --dry-run` 通过；提交 `f3e9dc2 Protect UI edits from silent refresh` 已推送。静态资源按 mtime 版本号读取，无需重启服务，刷新页面生效。 |
| 已销号 | 2026-07-05 吧唧登天阶可登不登 | 用户反馈“吧唧早就可以登天阶了，不登”。需要定位吧唧身份映射、`tianti_enabled`、天阶冷却/进度/问心/罡风状态、最近 `.天阶状态`/`.登天阶`/`.引九天罡风` 日志和 scheduler 条件。验收：若冷却已到且模块开启，应按现有安全锁发送登阶；若因配置、资源、问心/罡风前置或状态缓存阻断，日志群/UI 要明确展示阻断原因；补测试防止可登状态长期静默，也防止重复登阶风暴。 | 根因：吧唧 `tianti_enabled=0`，但 12:18 真实 `.天阶状态` 显示 5/12、罡风 12/12、登阶冷却可立即；此前 `tower_enabled=1` 只是闯塔，不等于登天阶。处理：先停 `xiuxian.service`，再通过 `set_module_enabled('登天阶', True, send_as_id=301299112)` 落库，避免运行中旧 worker 退出时用旧内存覆盖 DB；随后启动服务。验证：DB `tianti_enabled=1`；19:40 自动 `.引九天罡风` 成功，19:42 `.天阶状态` 同步识别可登并接续 `.登天阶`，19:42:38 登阶成功至 6/12，下次 22:42；pending=0，未见超时/安全锁/fuse。 |
| 已销号 | 2026-07-04 全模块超时证据回捞审计 | 按 WA 野外历练事故举一反三：`send_timeout`、`reply_timeout`、`send_queue_timeout` 不能直接等同“未发送/失败”。所有会在超时后补发、推进状态、释放下游、切下一阶段或写长 CD 的模块，都必须先从 `data/messages/*.log`/passive inbox/真实 reply/edit 按 `msg_id`、`reply_to_msg_id`、同身份近窗命令回捞证据；捞到成功/冷却/中间态则回补状态，捞不到才进入低风险校准（如状态面板/天机盘）或保守退避。高风险动作（野外深入、探寻裂缝、副本推进、储物袋交易等）禁止在状态不明时靠猜测继续。 | 2026-07-04 已先修野外历练：发送状态未知先等被动日志，未知超时后回捞同身份近窗 `.野外历练*` 命令和 reply/edit；CD 回复优先于结果识别；捞不到则查 `.天机盘` 校准，不再降级/放行高风险动作。随后补 `message_log_recovery` 只读共享工具，并接入探寻裂缝、钓鱼、储物袋转移、慕兰、婉心、斗法、问道、世界 boss：超时到期先按 command msg/reply/edit 回捞并喂回既有 handler，捞不到才走原保守退避、补查或校准。追加接入小世界、闯塔：判定超时前先按原 command msg/reply/edit 从消息日志回捞并回灌既有 handler，小世界覆盖护界、神迹、查询、显灵、收割、淬炼链路；闯塔覆盖 `.闯塔` pending。2026-07-18 继续发现轻量副本真实重复：落云加入命令 `222656/222701` 均在 1 秒内收到“队伍已经满员”直接回复 `222657/222702`，旧快重试仍发送 `222757/222805`。修复 `model/app_replica.py`，join/dissolve 快重试统一先按原命令 ID 和运行时游戏群 ID 查询直接 reply/edit；已有回复禁止补发，真正无回复仍最多一次。副本定向 `358 passed / 45 subtests`、全量 `3227 passed / 396 subtests`，提交 `0d1e927d` 已推送并上线。随后完成南陇、侍妾、极阴与 generic pending 审计：南陇安置/交易/召回均先按 `nanlong_last_msg_id` 回放；侍妾 status、赠予、入梦、残图、谜题、补领、代卜、心劫、远航均有 phase 到消息 ID 和既有 handler 的回放映射；极阴 `max_send_attempts=1` 不自动二发；generic retry 补发前调用 `_recover_pending_reply_from_message_log()`。联合回归 `185 passed / 7 subtests`。再修旧 `join_dungeon` 快补发、玄骨考校和天机考验：旧副本 join 已有 Bot 直接回复时不再快补发；两套答题在重发前扫描本次题面/作答后的广播或 edit 并回灌原 handler。提交 `20089892`，全量 `3230 passed / 396 subtests`。器灵剑阵、周天星斗、第二元神也已补业务回灌：剑阵在一次资源消耗补发前恢复直接回复；周天星斗同时恢复助阵命令回复和原邀请最终 edit；第二元神状态/修炼/镇魔/查魔染超时前均恢复命令与回复，`send_timeout` 缺消息 ID 时先找回已发送命令。提交 `a7e4e8c9`，全量 `3233 passed / 396 subtests`。再补灵树采摘与登天阶问心/罡风/状态 gate：在过期后重发前严格按当前身份、运行时游戏群、主题和原命令 ID 回捞 Bot reply/edit，并通过原业务 handler 闭合采摘、问心、罡风或状态；频道身份也复用共享身份匹配。提交 `d5943cbc`，全量 `3235 passed / 396 subtests`，`compileall`、diff check 通过；20:02 显式重启后 24 身份恢复成功、pending=0、health/watchdog ok。最终完成共享严格绑定收口：`message_log_recovery` 的 reply/message/recent-command 查询支持显式 `chat_id`，generic pending、phaseful 深闭/元婴及斗法、钓鱼、合欢、慕兰、南陇、小世界、天星、闯塔、婉心、问道、世界 boss、储物袋等恢复路径统一绑定运行时游戏群，跨群同号日志不再误绑；提交 `a20bbdd7`。补同 ID 跨群冲突测试，联合回归 `947 passed / 39 subtests`、全量 `3236 passed / 396 subtests`。静态确认点卯成功发送即排次日、传功漏回包只停链，灵树 MiniApp 四处状态变更 `run_start/run_submit` 均禁 HTTP 自动重试，passive inbox 无发送调用；线上观星台 MiniApp、洞府深闭均真实闭环。20:35 显式重启后 24 身份恢复成功、pending=0、health/watchdog ok，审计销号。 |
| 已销号 | 2026-06-30 修复后 12h 监测 | 本轮紧急修复上线后，不按短观察收尾；至少连续 12h 监测 health observer、safety watchdog、发送日志、天星路线日志、侍妾链路日志。发现风暴、少发、误发、错误放行、超时恢复异常，先记录到本账本再修复；不能边改边遗忘。 | 该特定 12h 观察窗已持续远超门槛，并在后续多轮线上修复、重启和真实业务窗口中持续复核。2026-07-15 销号时 `xiuxian.service`、listener、health observer、watchdog 均 active，health observer 持续 ok，WA 野外/裂缝/斗法、婉心和 MiniApp 均已有真实回包证据。此销号仅关闭旧的“修复后 12h”阶段任务，不代表停止长期监测修复循环；长期观察继续由共享发送/回包五项收口及各模块独立事项承担。 |
| 已销号 | lab 与 live 热加载边界隔离 | `/opt/xiuxian-main` 当前同时是开发目录和 `xiuxian.service` 的运行目录，`xiuxian.py` 主进程会检测代码变化并自动重启 worker；lab 改动可能在未显式上线/重启时被 live worker 加载。需要设计可审计的 lab/prod 隔离方案，至少包含：停止热加载或分离 lab checkout、上线命令边界、改动前后状态确认、避免开发期触发生产发送。 | 已改为显式 opt-in：`xiuxian.py` 的 `_hot_reload_enabled()` 默认读取 `XIUXIAN_HOT_RELOAD=False`，生产默认不热重载；若要热重载必须显式设 `XIUXIAN_HOT_RELOAD=1`，并已有 pending drain 保护。验证：`.venv/bin/python -m pytest -q tests/test_supervisor_pending_drain.py` 4 passed；2026-07-06 07:45 重启线上服务日志显示“代码热重载已关闭；生产变更需显式重启服务。”；同次重启后主服务 active、pending=0，未见导入/语法错误。后续开发仍建议使用分离 lab checkout，但生产热加载风险已收敛。 |
| 已销号 | 脚本架构/结构/安全规范吸收复审 | 当前上线后监测通过后再做：从脚本整体架构、三层结构、发送安全、安全锁/看门狗、被动证据、UI 配置、审计可追溯角度，对照现有脚本与 Rust/wxjerry 线，梳理可规范化、可吸收的安全做法；不能为吸收而扩大主动发送面。 | 复审已由 `docs/audit/upstream-absorb-review-20260716.md`、`shared-send-reply-checkpoint-20260716.md`、`full-audit-tail-closeout-20260722.md`、`miniapp-runtime-convergence-20260723.md` 和持久化 P1/P2 Gate 文档覆盖。已吸收差量持久化、双代 guard、严格身份绑定、被动回包恢复、MiniApp 统一 transport/capture、业务语义健康监测和 UI 请求边界；明确拒绝任意账号回退、轮换代理绕限流、共享锁无替代拆分和 CommandAttempt 越权接管。该规范复审完成，具体上游新功能继续在 wxjerry/Rust 独立事项跟进。 |
| 已销号 | WA 手动天星记录与本地状态校准 | 复盘 @WalterWA2000 2026-06-29 19:40 前后手动 `.改命 探索`、`.推命 探索`、`.野外历练 深入`、`.天机盘`：真实游戏可执行天星命令，但本地 DB 仍显示 `sect_name=散修`、`tianxing_enabled=0`、`tianxing_observation={}`；需要确认是身份宗门缓存过期、手动天星被动记录未保存，还是天星玩法允许退宗后延续，并据此补校准/审计。 | 2026-07-15 本地状态已校准为 `sect_name=天星宗`、`tianxing_enabled=1`，观测账本可持续记录命星、推命、改命、天机值和消费结果。WA 当日 00:03、12:06 两次裂缝及 20:07 野外均由真实 reply/edit 回包确认，天星偏转与推改状态正常落账；旧“散修且空观测”问题已不再存在。 |
| 已销号 | 2026-06-30 天星探索推命消费整改 | 用户指出 WA 野外失败/改命掉了后，探缝不应复用上一次探索推命；真实记录显示 `.野外历练 深入` 回包有 `【推命命中】`，随后 `.探寻裂缝` 只有 `【改命待发】`、无 `【推命命中】`。验收：探索下游动作一旦出现推命命中/落空，本地必须标记当前推命已被该动作消费，清空对应 release；下一次野外/探缝必须重新推命探索，不能只靠改命待发放行。 | `model/features/tianxing.py` 已落地 consumed prediction 与 release 消费门禁，测试覆盖命中/落空后清空和下一动作重新准备。2026-07-15 WA 00:03、12:06 裂缝均分别出现新的 `【推命命中】`，20:07 野外再次独立消费推命，证明不同探索动作未复用已消费推命；全量回归 `3088 passed, 396 subtests passed`。 |
| 已销号 | 2026-07-01 昆吾山日志反馈不全 | 用户反馈昆吾山流程日志不完整。后续需复盘 2026-07-01 WA 昆吾山真实运行记录，核对自动抉择、进入/组队、楼层推进、奇遇选择、结算/失败/解散、奖励通报是否都有管理员可读日志；补缺时要沿用副本三层结构和现有 `app_replica` 证据链，避免增加无意义主动查询。 | 2026-07-17 已与封魔塔新文案适配合并销号：新版阶段提示进入原有副本三层证据链，自动发送仍带严格身份、房间、阶段去重和失败按钮兜底；结算标题进入房间清理、CD、成果日志与稀有日结。后续只等下一次真实昆吾观察，不主动开本探测。 |
| 已销号 | 2026-07-04 昆吾山封魔塔新文案适配 | 读取 2026-07-04 00:01-00:08 真实更新：结算标题已出现 `昆吾山·封魔塔镇压成功`，帮助/奖励新增或确认 `昆吾灵钥`、封魔塔遗响门、五宝解封门槛、幸运掉落权重与 `大衍诀残篇·控傀` 等；自动抉择必须按阶段区分 `路径1/2/3/4`、`1/2/3`、`镇/夺/断`。后续修昆吾日志/副本重构时要用新文案做回归样本，不能只识别旧 `登顶昆吾山`。 | 2026-07-17 按 7/16 真实样本完成：严格解析第一幕、动态编号第二幕和三处阵眼，默认遗响路线为 `路径1 → 太阴火窟（无则灵缈园救援）→ 八灵镇 → 化龙夺 → 黑风镇`；选项按标签匹配，阶段未知 fail-closed。补 `昆吾山·封魔塔镇压成功` 结算识别及封魔塔奖励池稀有物资统计。副本定向 `412 passed, 16 subtests`，全量 `3130 passed, 396 subtests`。 |
| 进行中 | 2026-07-02 苍坤洞府/整体副本链路重构评估 | 用户反馈苍坤洞府副本日志群消息、加入副本、群内副本推进几乎乱序：广播等待慢、加入慢、体验不清爽，且经常发送缺失、顺序不对、卡顿。后续需整体审计 `app_replica` 副本三层结构：广播监听、房间/队伍状态机、加入排队、职业分配、群内推进、日志群通报、缺失发送补偿和慢路径；苍坤优先，但要评估昆吾/落云/坠魔/黄龙等副本是否共享同一结构缺陷，避免头痛医头。 | 已落地多队职业/神识/门票规划、用户名历史解析、手动选择回写、加入/解散回复回捞、终态静场释放和日志群队伍预览。2026-07-29 新增只读 AST 边界工具 `tools/replica_boundary_report.py` 与审计 `docs/audit/replica-boundary-audit-20260729.md`：原文件 14692 行、616 函数、17 个生产入口；轻量房间域与虚天殿状态域存在 100/66 个双向跨区调用，禁止按大段直接拆。Gate A 已将黄龙征召实际抽入 `model/features/replica_huanglong.py`，原函数 facade 和 patch 路径保持兼容，未知发送只记尝试并按一小时退避。2026-07-30 Gate B 新增 `model/features/replica_panel.py`，把只读快照、总览/CD/帮助格式化与 `control.py` 依赖从巨型模块剥离；房间写入、按钮动作和 reducer 仍由 `app_replica.py` 单一持有。定向 `308 passed, 45 subtests passed`，AST 报告确认 `control.py` 只剩一个带按钮面板构建入口。下一步做 Gate C 窄 `ReplicaContext`；最后才处理房间生命周期/路线核心。 |
| 已销号 | 2026-07-11 副本静场令终态即时释放 | 当前北冥小极宫等副本已经结束后，静场令仍按固定 TTL 阻断普通模块。需要沿真实进入失败、房间解散、副本结算/结束回包立即清理 `dungeon_quiet_until/reason`，固定 TTL 仅作无终态回包时的安全兜底；补跨副本回归，避免旧房间静场阻断后续天星、野外、合欢等正常调度。 | 根因是释放器只识别旧终局词，遗漏真实 `【北冥小极宫·寒宫失利】` 的“本次未能带出/全员获得/最终状态”结构。已补副本终局结构与解散词识别，保留中间幕“当前状态”不释放；与苍坤加入推荐、tower/checkin 回归合计 `272 passed, 7 subtests`，提交 `a27621d3` 已上线并 push。 |
| 已销号 | 2026-06-30 储物袋洗灵石聚合挂单整改 | 用户指出洗灵石不应每个号反复上架；正确链路是目标号上架固定数量/单价，例如 `.上架 黄芽丹*20 换 灵石*600000`，其他号按份数 `.购买 ID*n`。UI 需显式配置上架数量和单价，总价自动反算，不能自动改用户定价/数量。 | 实现与生产证据见 `docs/audit/storage-bag-aggregate-listing-closure-20260728.md`：目标身份仅单次挂单，来源身份按 `.购买 <ID>*<份数>` 串行分摊；真实挂单 `24181` 完成 5/5、`24182` 完成 11/11，每步均有发送 ID、Bot 成功回包和库存核销。focused `95 passed`，当时全量 `3374 passed, 535 subtests passed`；提交链自 `889ee301` 起已包含后续超时与回包恢复修复。 |
| 已销号 | 2026-06-30 世界 boss 真实链路整改 | 旧文字链路读取 22:25-22:28 真实记录：开启后外部 `.世界boss` 与行动回包能提供战况；本方第一轮 23 身份从 22:25:42 到 22:26:48，只确认破幡/镇魂，第二阶段强攻窗口 22:27:37-22:28:07 被 90s round gap/cooldown 错过。2026-07-09 新版链路已迁移为 MiniApp 入场：开场文案明确“点击下方按钮进入真仙战场”“本轮废弃破幡、镇魂、护阵，强攻即为主要攻击”。验收更新：旧文本破幡/镇魂/护阵不得在新版开场后自动恢复；开场只做高优提醒和候选统计；结算要解析贡献榜/保底收获；后续若实现自动入场，必须按登录账号去重，每个账号只选一个角色，最多 4 个账号，并且走 MiniApp 协议抓包/mock/lab 灰度。 | 2026-06-30 已实现旧链 priority window 中断逻辑。2026-07-09 追加整改：`model/features/world_boss.py` 新增新版 MiniApp-only 开场识别，MiniApp-only 状态下 `.世界boss` 状态/行动回包不再复活旧文字行动链；新增贡献榜/保底结算解析，日志群可播本方上榜与物资收益；新增 `select_world_boss_miniapp_entry_identities()` 按登录账号去重并限制最多 4 个候选。验证：`.venv/bin/python -m pytest -q tests/test_world_boss.py tests/test_webapp_core.py` 111 passed / 10 subtests，`py_compile`、`git diff --check` 通过。2026-07-18 负债复核：自动入场与出手已完成生产协议灰度；7月17日4个登录账号全部入场并4/4结算，有效强攻64、完美64，且单账号内部串行、全局最多4账号。旧破幡/镇魂/护阵链未复活，结算榜与本方收益正常，验收完成。 |
| 已销号 | 2026-07-03 rust 线更新对照吸收 | 用户反馈 rust 线又有更新。需要先只读对照 rust 线最近提交/差异，归纳可吸收的精华，重点看发送安全、监听去重、超时恢复、钓鱼/天星/慕兰/副本等模块状态机是否有更稳做法；吸收前必须说明取舍，不整块搬运，不扩大主动发送面，不绕过安全锁/看门狗。 | 已由 `docs/audit/upstream-absorb-review-20260716.md` 对 Rust `c4bbc85` 完成结构、发送安全、监听、持久化、账号门禁、频道身份和 Bot 学习策略的逐项对照。持久化 P1 已于 2026-07-22 经 `docs/audit/persistence-p1-gate-review-20260722.md` 验证并上线；P2、账号目标群成员门禁等未完成候选继续归入“wxjerry/Rust 更新吸收”主项，不再保留重复待办。 |
| 已销号 | 2026-07-17 wxjerry 线与 MiniApp 代理池可行性评估 | 只读核对 wxjerry 线近期更新，重点评估 MiniApp HTTP/WebView 是否需要代理池、代理与 Telethon `RequestMainWebView` 会话/`initData` 的绑定关系、入口与账号粘性、限流和 429 退避、失败切换、审计脱敏及代理池故障时的 fail-closed 行为。先出价值/风险报告和 Lab 方案，不接生产代理，不用代理池绕过游戏限流、风控或安全锁；只有通过独立 Gate、模拟测试和小范围授权后才考虑上线。 | 结论见 `docs/audit/miniapp-proxy-pool-feasibility-20260722.md`：本机当前未生成 Requests 代理配置，世界 Boss 已按身份复用并关闭 Session，线上实际直连；轮换代理池会放大非幂等重放和绕限流风险，不进入生产。只保留“默认直连、连接建立失败后切单个配置代理”的独立 Lab 候选，429/业务限流绝不换代理，全局仍硬守 90 次/分钟；当前无配置和故障证据，不改 runtime。 |
| 已销号 | 天星主动攒天机点与深度闭关接管 | 天星需要可主动通过 `.闭关修炼`、斗法等链路命中推命来获取天机点；其中闭关修炼与深度闭关互斥，不能两个模块同时抢发送。需要设计“天星 farm 窗口”：若正在深度闭关且需要普通闭关攒点，应由深度闭关模块受控交接，必要时 `.强行出关` 后等 CD 或在配置允许时 `.服用 合气丹` 再闭关修炼；天机点目标拿满后再把调度权交还深度闭关。全流程必须保序、走安全锁、可审计，默认 dry-run/lab。 | 2026-06-29 完成：`model/features/tianxing.py` 新增普通闭关攒点状态机，默认关闭且 dry-run；闭关路线必须走天星 timeline 预检，发送 `.闭关修炼`、`.服用 合气丹`、`.强行出关` 均走正常 `send_game_command`/安全锁/action_guard，不绕过全局锁。`model/features/deep_retreat.py` 在深度闭关即将发起时调用天星 gate；天星接管时延后深度闭关，不接管时才放行深度闭关；强行出关总结真实文案里的“需调息40分钟方可进行下一次【闭关修炼】”会回写天星攒点冷却。`runtime`/`module_manifest`/`action_guard` 已登记 `tianxing_retreat_farm`、`.闭关修炼`、`.服用 合气丹`、`.强行出关`，UI 展示攒点 phase/next_time/target/result。验证：`.venv/bin/python -m pytest tests/test_tianxing.py tests/test_phaseful_summaries.py tests/test_module_manifest.py tests/test_safety_watchdog.py tests/test_message_evidence.py tests/test_app_scheduler_contract.py tests/test_ui_dual_track.py -q` 279 passed / 49 subtests passed；`node --check model/web/static/js/module_cards_ui.js`、`compileall`、`git diff --check` 通过。 |

补充进行记录（2026-07-04 07:14）：全模块超时证据回捞继续补合欢温养：`model/features/hehuan.py` 在 `auto_pending_msg_id` 超时后，先按原命令消息从消息日志回捞温养成功、冷却或结算中回复，并回灌 `apply_hehuan_passive`；捞到真实回包则不补发 `.双修 温养`。验证：`.venv/bin/python -m pytest -q tests/test_hehuan.py tests/test_app_scheduler_contract.py tests/test_safety_watchdog.py` 169 passed / 4 subtests passed；`py_compile` 与 `git diff --check` 通过。

补充进行记录（2026-07-04 07:18）：全模块超时证据回捞继续补南陇侯确认链：安置侍妾、交换结果、召回侍妾这些已有 `nanlong_last_msg_id` 的确认阶段，在超时重试或降级前先按原命令消息从消息日志回捞回复并回灌 `handle_nanlong_reply`；提示本身过期仍按原保守逻辑，不做过期后自动选择。验证：`.venv/bin/python -m pytest -q tests/test_nanlong.py tests/test_app_scheduler_contract.py tests/test_safety_watchdog.py` 148 passed / 3 subtests passed；`py_compile` 与 `git diff --check` 通过。

补充进行记录（2026-07-04 07:24）：修复小世界神迹发送未知误判回复超时。真实日志确认 WA 07:17:58 没有 `.神迹 布道` sent/message，旧逻辑却先进入 `preach_pending` 并在 07:19 报 `消息ID=未知`。修复 `model/features/small_world.py`：神迹发送中仍临时占位防并发；若 `send_game_command` 未返回消息，立即清理 `preach_pending`、恢复上一次真实神迹发送时间、不写“已发送”等待回执，保留 `pending_god_action` 并按短退避重试；旧 `preach_pending + msg_id=0` 状态到期时静默转短退避，不再刷未知消息ID告警。验证：`.venv/bin/python -m pytest -q tests/test_small_world.py tests/test_retry_scheduler.py tests/test_safety_watchdog.py tests/test_app_scheduler_contract.py` 227 passed；`py_compile` 与 `git diff --check` 通过。

补充进行记录（2026-06-30 08:25）：天星真实文案复审完成。6/30 日志里观命、定命、推命、改命、天机盘、推命命中/落空、改命待发/回天、已有推命/改命冷却、消劫成功/无劫均已解析；补 `【天星宗 · 司命推演】`、成功拜入天星宗、已是天星宗弟子三类真实文案分类。根因确认：WA 07:30 已有 `current_prediction=探索/current_change=探索` 后，旧炼制放行仍触发 `.炼制 玄铁剑`，导致 `【推命落空】逆命劫 +1`；另一个结构缺口是炼制攒点只避让“到期前后 60 秒”的野外/探寻裂缝窗口，overdue 后反而可能抢跑。修复 `model/features/tianxing.py`：探索消费窗口只要已到期或进入准备窗就让炼制让路；已有推命/改命冷却、无逆命劫等真实文案校准状态；放行路线按最新观测裁剪。补 `tests/test_tianxing.py` 覆盖真实帮助/入宗文案、已有推命/改命、无劫、overdue 探寻裂缝让路。

补充进行记录（2026-06-30 08:50）：按用户要求再次核对 6/29-6/30 天星真实文案，670 条候选里核心链路可解析；发现漏项 `你并非天星宗弟子，司命盘不会为你显化命轨。` 之前会归为未知文案。修复 `model/features/tianxing.py`：明确识别为 `天星身份/not_member`，用于非天星身份或宗门缓存异常时阻断接管；补 `tests/test_tianxing.py` 回归。周天星斗大阵、斗法战报、下注等含“命中/天星/星宫”的旁路仍不进入天星核心状态。

补充进行记录（2026-06-30 09:00）：监测 WA 08:23 探寻裂缝前置窗口发现真实问题：探缝到期前遇到 `current_prediction=炼制` 时，旧逻辑按 `blocked_until + 60s` 把 `next_explore_rift_time` 推到 16:16，即使 08:19:55 炼制已命中并清空推命，也不会再重试本次探缝。修复 `model/features/explore_rift.py`：天星预检因异路推命阻断时，若探缝已到期则只按 `RETRY_MAX_SEC` 短延迟；若在前置窗口内则保留原 due_at 或短重试，不再吃掉探缝 CD。补 `tests/test_explore_rift.py` 覆盖已到期和前置窗口两种场景。

补充进行记录（2026-06-30 08:40）：复核图图天星野外状态，确认 08:05-08:37 真实日志只有 `.推命 炼制` / `.炼制 玄铁剑`，未跑 `.推命 探索`、`.改命 探索`、`.野外历练 深入`；DB 显示 `tianji_value=42`、`current_change=闭关`、`next_wild_training_time=2026-06-30 10:37:06`、野外策略为谨慎。修复 `model/features/wild_training.py`：野外天星消费窗口显式 `require_change_fate=True`；无探索改命、天机不足或已有异路改命待发时，到期野外降级 `.野外历练 谨慎`，不发深入，也不因等待探索改命而吃掉野外 CD。补 `tests/test_wild_training.py` 覆盖“已有闭关改命时野外降级谨慎”和未来窗口短重试边界。

补充进行记录（2026-06-30 08:40）：侍妾“几何级数/同名墨彩环”风险复审完成。真实日志里外部账号同名墨彩环面板不是几何级数本人；侍妾被动路由应依赖 `send_as_id`/待回复消息链，而不是侍妾名。修复 `model/features/concubine.py`：非等待期状态 handler 若缺少 `.我的侍妾` 命令锚点，不接受 `concubine_status` 面板，避免后续路由误传 family 时污染身份；补 `tests/test_concubine_affinity.py` 覆盖“外部同名面板无身份上下文不写入”和“idle 缺命令锚点不接收”。验证：`PYTHONPATH=. .venv/bin/pytest tests/test_tianxing.py -q` 130 passed / 4 subtests passed；`PYTHONPATH=. .venv/bin/pytest tests/test_concubine_affinity.py -q` 119 passed；`PYTHONPATH=. .venv/bin/pytest tests/test_tianxing.py tests/test_explore_rift.py tests/test_wild_training.py tests/test_concubine_affinity.py tests/test_phaseful_summaries.py tests/test_health_observer.py -q` 407 passed / 9 subtests passed；`compileall`、`git diff --check`、JS `node --check` 通过。生产仍停机，待启动后进入 12h 监测。

补充销号记录（2026-06-29 09:20）：按用户要求追加天星深度 lab 审计，覆盖真实文案、文案轻微变化、未知文案、吞回复、超时、迟到回复、陈旧回复、普通闭关攒点吞回复/迟到回复、强行出关冷却回写、合气丹和安全边界；详见 `docs/lab_tianxing_audit_20260629.md`。新增/加固 `tests/test_tianxing.py` 场景，放宽等价文案 `推下了一段命数`、`预留一次改命回天` 的解析；验证：`.venv/bin/python -m pytest tests/test_tianxing.py tests/test_phaseful_summaries.py tests/test_module_manifest.py tests/test_safety_watchdog.py tests/test_message_evidence.py tests/test_real_message_replay.py tests/test_app_scheduler_contract.py tests/test_ui_dual_track.py -q` 302 passed / 51 subtests passed；`node --check`、`compileall`、`git diff --check` 通过。
| 已销号 | UI 删除模块区冗余标题 | 首页模块区不再显示“模块详情 / 当前身份的自动化模块”，直接展示模块卡片 | 修复 `model/web/pages/index.html`：删除 `section-head-row` 标题块；补 `tests/test_ui_dual_track.py::test_render_default_ui_uses_new_skin_without_mode_switch` 断言页面仍有 `module-grid` 且不含冗余文案 |
| 已销号 | UI 小世界护界开关收进设置 | 小世界卡面顶部不再显示护界开关；护界开关与护界阈值、提前分钟、最小间隔在同一个设置分块中调整 | 修复 `model/web/static/js/module_cards_ui.js`：`primaryTools` 只保留显灵/收割，`renderSmallWorldFeature(identity,'barrier','护界')` 移入“护界禁制策略”设置分块；测试 `tests/test_ui_dual_track.py::test_module_card_override_groups_settings_and_moves_dense_toggles_into_modal` 覆盖 |
| 已销号 | UI 模块聚合与设置二次统一 | 法宝无主开关，仅抚摸/温养/试炼三开关并放入设置；日常按玩法逐项设置，点卯/闯塔窗口不再二次弹窗；奇遇显示当前选择并删除错误说明；问天/斗法归入日常；元婴与探寻裂缝组成元婴卡；垂钓设置按钮统一叫“设置” | 修复 `model/web/static/js/module_cards_ui.js`：法宝名称内联保存，日常加入 `卜筮问天`/`斗法` 并移出 `元婴`，新增元婴聚合卡，奇遇设置内显示当前极阴/南陇选择，删除“献魂偏收益/收敛偏保守/不确定收益”错误文案；点卯/闯塔窗口改为设置弹窗内联保存。修复 `model/web/static/js/fishing_ui.js`：垂钓入口按钮改为“设置”。补 `model/web/static/css/ui_fixes.css`、`model/web_new/static/css/app.css` 内联窗口/当前选择/名称输入样式；测试 `tests/test_ui_dual_track.py`、`tests/test_fishing_ui.py` 覆盖 |
| 已销号 | 小世界有祈愿时不再显灵前收割 | 面板已出现凡人祈愿且自动显灵开启时，直接发送 `.显灵`；`.收割香火` 只在无祈愿维护链路中独立执行 | 修复 `model/features/small_world.py`：`has_prayer` 分支不再调用 `_send_harvest_before_manifest`，保留无祈愿时独立收割链路；测试 `tests/test_small_world.py::SmallWorldTests::test_prayer_panel_manifests_without_harvest_when_incense_ready` 覆盖待收香火充足也直接显灵 |
| 已销号 | UI 日常/奇遇开关收进设置 | 日常与奇遇卡片顶部只保留设置入口，具体子功能开关进入模块设置弹窗 | 修复 `model/web/static/js/module_cards_ui.js`：日常六个子开关进入“日常功能开关”设置分块，奇遇三个子开关进入“奇遇功能开关”设置分块；测试 `tests/test_ui_dual_track.py::test_module_card_override_groups_settings_and_moves_dense_toggles_into_modal` 覆盖 |
| 已销号 | UI 法宝设置直达 | 法宝名称设置不再通过模块设置弹窗二次跳转，卡片入口直接打开法宝名称设置 | 修复 `model/web/static/js/module_cards_ui.js`：法宝卡片直接渲染 `data-open-pet-modal` 设置按钮，不再通过通用模块设置弹窗跳转；同一 UI 测试与 Node mock 渲染覆盖 |
| 已销号 | UI 命令读取补元婴/第二元神等级 | `.元婴状态` 可写入元婴等级；`.第二元神` 可写入第二元神等级；`.五子同心魔` 只查魔染不覆盖等级；UI 角色卡展示该来源 | 纠偏 `model/control.py`：`游戏命令读取` 发送 `.我的灵根`、`.元婴状态`、`.第二元神`；收紧 `model/identity_levels.py` 和 `model/features/second_soul.py`，第二元神等级只从第二元神面板写入。测试 `tests/test_passive_identity_profile.py::PassiveIdentityProfileTests::test_refresh_identity_info_sends_level_read_commands`、`tests/test_second_soul.py`、`tests/test_phaseful_summaries.py::PhasefulSummaryTests::test_yuanying_status_reply_writes_level_for_ui_even_when_module_disabled` 通过 |
| 已销号 | 第二元神 CD/等待态显示纠偏 | “修炼中但无剩余时间”的短复查不显示成修炼结束；心魔 pending 的 deadline 丢失时能自愈补齐，不长期卡死 | 修复 `model/features/second_soul.py`：短复查显示为“下次复查”；`heart_demon_pending` 缺失 deadline 时补齐 1h deadline。测试 `tests/test_second_soul.py::SecondSoulTests::test_busy_training_without_remaining_is_short_recheck_not_end_time`、`tests/test_second_soul.py::SecondSoulTests::test_heart_demon_missing_deadline_is_repaired` 通过 |
| 已销号 | 落云秘圃归属与结算审计修复 | 不把别人的落云秘圃写入当前账号日志；开本/加入/结算/CD 归属都有可测试约束 | 修复 `model/app_replica.py`：真实 `【落云秘圃·第一幕·破禁入圃】` 识别为进入；带队伍用户名的落云进度必须与本地房间用户名有交集，不再退回最新本地房间；补 `tests/test_replica_absorb.py::ReplicaAbsorbTests::test_luoyun_other_team_first_stage_does_not_enter_room_without_username_evidence`，落云关键 6 测试通过 |
| 已销号 | 落云秘圃 CD 到期提醒 | CD 到期后每小时 `@管理员` 提醒一次；用户重新进入 CD 后停止提醒 | 新增 `run_luoyun_cd_reminder_scheduler` 并接入全局调度；测试 `tests/test_replica_absorb.py::ReplicaAbsorbTests::test_luoyun_cd_reminder_repeats_hourly_until_new_cooldown` 通过 |
| 已销号 | 副本日志补组队情况通知 | 副本日志通知包含队伍状态；满 5 人时通知管理员 | `_mark_replica_team_joined_from_text` 增加队伍进度/满员通知，满员走 high priority；测试 `tests/test_replica_absorb.py::ReplicaAbsorbTests::test_replica_team_full_notice_is_high_priority_and_deduped` 通过 |
| 已销号 | `.黄龙征调` 每日提醒 | 每天通知轮值宗门并 `@管理员`；不重复刷屏 | 新增 `CMD_HUANGLONG_CONSCRIPTION`、被动解析和 12:05 后兜底查询调度；测试 `tests/test_replica_absorb.py::ReplicaAbsorbTests::test_huanglong_conscription_notice_parses_real_reply_once`、`tests/test_replica_absorb.py::ReplicaAbsorbTests::test_huanglong_conscription_scheduler_queries_once_after_noon` 通过 |
| 已销号 | 钓鱼 23:40 买饵与 0 点启动保障 | 每天约 23:40 买饵；0 点后至少 1-2 个账号开始打窝/垂钓；不撞安全锁 | `model/features/fishing_behavior.py` 增加日切备饵/待命状态：23:40 起只做缺饵购买或待 0 点；0 点清旧日计数后启动新竿；`model/features/fishing_runtime.py` 将买饵/打窝/垂钓都纳入最多 2 个身份启动容量。`tests/test_fishing_runtime.py` 全 44 个通过，钓鱼全套 125 个通过 |
| 已销号 | 共历心劫抉择无回合推进 | 查清 `.稳` 已记录发送但天尊无后续 edit/结算时的根因；避免误按长冷却收口，也不能重复刷旧 prompt | 根因：2026-06-27 18:16:10 天尊真实回复 `心劫锚点已散，需重新引动天劫。`，但该消息是 topic-root / 无 reply context，未被心劫 handler 路由，脚本随后按“无回合推进”超时收口。修复 `model/features/passive_inbox.py`：无 context 时仅在唯一活跃心劫 prompt 存在时路由给该身份；修复 `model/features/concubine.py`：识别锚点散失后清理旧 prompt、转 `.我的侍妾` 状态校准，并支持 scheduler 从 message log 补读该文案；避免重复统计 passive 事件。测试：`.venv/bin/python -m pytest tests/test_concubine_affinity.py -q` 117 passed；`.venv/bin/python -m pytest tests/test_startup_recovery_guards.py tests/test_concubine_affinity.py -q` 145 passed；全量 `.venv/bin/python -m pytest -q` 1715 passed / 341 subtests passed；`compileall`、`git diff --check` 通过 |
| 已销号 | 天尊消息已发但脚本读不到 | 找到真实日志根因；副本卡住时有恢复或补读策略 | 根因拆分为两类误报：2026-06-27 17:54 `11043178 -> 11043180/11043185` 是 `.我的灵根` 的身份玉牒中间态/不完整玉牒，脚本已收到但因还需战力补全被记为 `identity_info` handler gap；2026-06-27 18:21 `11044363 -> 11044369` 与 `11044383 -> 11044387` 是已知身份手动 `.上架/.购买` 被 manual reply tracker 路由，当前无储物袋转移任务时误记 handler gap。修复 `model/app.py`：`identity_info` 等待态不清 pending、不记 gap；玉牒/战力评估作为已读观察，不再记未处理；手动储物袋交易回执降级为 `manual_storage_trade_observation`，不掩盖真实 handler gap。测试：`.venv/bin/python -m pytest tests/test_passive_identity_profile.py tests/test_storage_bag_transfer.py tests/test_message_contract.py tests/test_runtime_reply_context.py -q` 89 passed；`git diff --check` 通过 |
| 已销号 | wxjerry/rust 模块差异复审 | 输出可吸收项清单；只合入安全且本仓库适配的逻辑 | 复审结论：不整块搬运 wxjerry/rust；世界 boss、储物袋 gift/batch/retry、副本证据驱动、message-box/passive lessons 已由本仓库现有三层结构覆盖或更严格。本轮仅吸收安全增量“钓鱼鱼获 -> 储物袋赠送队列”：默认关闭；UI 可选择赠送目标；提竿成功后记录 `fishing_caught_fish_json`；空闲且到期时由 `model/features/fishing_runtime.py` 调用 `start_storage_bag_gift_batch`，不直接发 `.赠送`；储物袋忙时进入其批量队列；待赠鱼获优先于满竿后自动开鱼。代码位置：`model/features/fishing_behavior.py`、`model/features/fishing_runtime.py`、`model/state.py`、`model/persistence.py`、`model/ui.py`、`model/web/static/js/fishing_ui.js`。测试：`.venv/bin/python -m pytest tests/test_fishing.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_storage_bag_transfer.py -q` 175 passed；`.venv/bin/python -m compileall -q model tests` 通过；`git diff --check` 通过；全量 `.venv/bin/python -m pytest -q` 1723 passed / 341 subtests passed。上线观察：2026-06-28 01:48 重启 `xiuxian.service` 后 `xiuxian.service`/`xiuxian-safety-watchdog.service` active，`global_enabled=1`，`tools/health_observer.py --once` 返回 ok，`tools/safety_watchdog.py --once` 返回 `watchdog ok`，重启后主服务错误扫描为空，未见 `data/state/safety_watchdog_fused.json` |
| 已销号 | Rust 钓鱼命令/UI 二次对照 | 对照 Rust `agent/claude/fishing-daily-plan` 的命令链和可调项；只吸收能并入当前三层结构且不扩大主动发送面的内容 | 2026-06-29 已对照：Rust 命令链含 `.鱼篓`、`.买鱼饵`、`.打窝`、`.钓鱼`、`.试探咬饵`、`.提竿`、`.收竿`、`.开鱼`；UI/配置侧可取项为单竿硬超时、开鱼保留量、日志抑制、日计划预算。本轮只吸收最小安全项：新增 `.收竿` 命令常量/reply family/watchdog 短链白名单，新增“卡竿收竿”UI 秒数设置，单竿超过阈值发 `.收竿` 兜底；修复钓鱼发送防重误把 `next_fishing_time` 当已发证据导致 `.收竿` 被自身 phase 拦截；补 runtime schema-ready guard，避免新增字段热加载时先 upsert 后 migration。不吸收日志抑制、开鱼保留量、日计划预算。验证：`.venv/bin/python -m pytest -q tests/test_fishing.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_safety_watchdog.py tests/test_persistence_runtime_flags.py` 273 passed；2026-07-06 07:45 重启后生产加载正常，DB 确认 24 个身份 `fishing_enabled=0`，旧文本钓鱼不放量；后续实际钓鱼放量转 MiniApp 待办。 |
| 已销号 | UI 模块卡片滚动条与上半区设计统一 | 固定模块卡高度后，模块内部滚动区应自适应卡片剩余高度；统一各模块上半部分设计语言；评估总开关是否必要，是否应改展示具体功能开关；奇遇的献魂/收敛等、小世界细项是否应收进设置面板；顶栏全局/日志等控件与 Xiuxian 图标同一行，不另起一行 | 设计结论：保留总开关，但只表达聚合模块主模块启停；子功能开关留在卡片顶部紧凑开关组；低频策略按钮/阈值输入/同步动作收进统一 `设置` 折叠区。实现：新增 `model/web/static/js/module_cards_ui.js` 覆盖首页模块卡渲染壳，保留原有 API 和点击处理；`model/web/static/css/ui_fixes.css` 将模块卡固定为 260px 行高，`.module-detail` 使用 `flex:1/min-height:0/overflow:auto` 自适应剩余高度，不再固定 `max-height:170px`；同步 `model/web_new/static/css/app.css`，避免新皮肤覆盖；顶栏在 1360px 以下保持 52px 单行，按钮区横向滚动，不再把全局/日志/配置拆成第二行。验证：`node --check model/web/static/js/module_cards_ui.js` 通过；`.venv/bin/python -m pytest tests/test_ui_dual_track.py tests/test_fishing_ui.py tests/test_storage_bag_ui.py tests/test_yinluo_ui.py tests/test_account_logout_ui.py -q` 27 passed；`.venv/bin/python -m compileall -q model tests` 通过；`git diff --check` 通过；全量 `.venv/bin/python -m pytest -q` 1725 passed / 341 subtests passed；用 Chromium fixture 截图检查 1280x800 与 390x844，顶栏单行、模块卡顶部收敛、卡内详情区自适应滚动。上线观察：2026-06-28 02:04 重启 `xiuxian.service` 后 `xiuxian.service`/`xiuxian-safety-watchdog.service` active，`global_enabled=1`，`tools/health_observer.py --once` 返回 ok，`tools/safety_watchdog.py --once` 返回 `watchdog ok`，重启后主服务错误扫描为空，未见 `data/state/safety_watchdog_fused.json`；live 静态资源可访问 `/static/js/module_cards_ui.js`，`/static-new/css/app.css` 已包含 260px 模块卡与 52px 单行顶栏规则 |
| 已销号 | UI 模块设置统一弹窗化 | 卡片内不再使用折叠设置；所有模块低频设置统一为类似“灵溪垂钓设置”的弹窗；弹窗内说明文字更完整，避免非全屏显示不全 | 修复 `model/web/static/js/module_cards_ui.js`：低频设置改为通用 `module-settings-modal` 弹窗，卡片只保留“设置”按钮；法宝、日常、奇遇、观星台、登天阶、小世界、第二元神、太一设置均按“说明 + 控件”分块。修复 `model/web/static/css/ui_fixes.css` 与 `model/web_new/static/css/app.css`：删除旧 `details.module-settings` 折叠样式，新增弹窗卡片、滚动主体、移动端单列控件样式。测试：`node --check model/web/static/js/module_cards_ui.js` 通过；`.venv/bin/python -m pytest tests/test_ui_dual_track.py tests/test_fishing_ui.py tests/test_storage_bag_ui.py tests/test_yinluo_ui.py tests/test_account_logout_ui.py -q` 27 passed；`.venv/bin/python tools/ui_http_smoke.py` 13 checks ok；Chromium 390x844 mock 页面打开小世界设置：`buttonCount=6`、`hasDetails=false`、`modalShown=true`、`sectionCount=2`；`compileall` 与 `git diff --check` 通过 |
| 已销号 | 天星宗玩法开发计划报告与一期自动化 | 摸清天星玩法全流程、真实文案、自动化流程、UI 可调项、补发策略、三层结构、可维护/可审计设计；先交付报告，不直接扩大生产发送 | 已交付报告；一期实现 `tianxing_auto_config`：查盘/观命/消劫保守自动，定命/推命/改命默认关闭且 dry-run 默认开启；策略支持命星优先级、路线优先级、推命/改命路线、天机阈值、逆命劫阈值、校准间隔；接入普通身份调度并置于野外历练前；UI 天星宗设置弹窗可配置并展示观测状态/最近自动计划；战略动作复用 `build_tianxing_manual_plan`，不绕过冷却/资源/脏字段校验。测试：`.venv/bin/python -m pytest tests/test_tianxing.py tests/test_app_scheduler_contract.py tests/test_ui_dual_track.py -q` 46 passed / 4 subtests passed；`.venv/bin/python -m pytest tests/test_control_bool_coercion.py tests/test_module_manifest.py tests/test_message_evidence.py -q` 71 passed / 46 subtests passed；`node --check model/web/static/js/module_cards_ui.js` 与 py_compile 通过 |
| 已销号 | 散修身份阻断宗门玩法 | 身份宗门为空/散修时，点卯、宗门传功、闯塔和所有宗门专属玩法不显示、不可手动开启、启动恢复/调度前会清理旧状态；不能靠撞天尊回复后才停 | 修复 `model/state.py`：空宗门/散修统一视为无宗门，移除点卯、宗门传功、闯塔及宗门专属模块；修复 `model/control.py`：手动开启返回无宗门原因，启动恢复清理旧宗门模块状态，补合欢宗关闭清理；修复 `model/features/checkin.py`：调度前门禁清理点卯/传功旧 pending；三宗门调度加身份宗门 preflight。测试：`.venv/bin/python -m pytest tests/test_control_bool_coercion.py tests/test_checkin_no_sect.py tests/test_tianxing.py tests/test_hehuan.py tests/test_yinluo.py -q` 154 passed / 13 subtests passed；`.venv/bin/python -m pytest tests/test_replica_absorb.py tests/test_app_scheduler_contract.py tests/test_log_group_display.py tests/test_module_manifest.py -q` 295 passed / 45 subtests passed；全量 `.venv/bin/python -m pytest -q` 1753 passed / 343 subtests passed；`compileall` 与 `git diff --check` 通过 |
| 已销号 | 天星宗嵌入式架构 lab 报告 | 2026-06-29 必须写出完整方案：定命前提、推命攒天机、改命服务野外/裂缝/闭关/斗法；包含真实文案、路线时间线、UI 可调、补发、安全锁、三层结构、审计点；先在 lab，不接生产实跑 | 已新增 lab-only 路线预检骨架和 `docs/lab_tianxing_architecture_20260628.md`；2026-06-28 已补 `docs/lab_tianxing_explore_integration_20260628.md`，沉淀真实文案口径，并在 lab 把天星 `探索` 预检接入 `探寻裂缝`、`野外历练` 发送前链路：未定命先定命，已定命优先 `改命 探索`，不再由模块前置盲插 `推命`。新增逆命保护：若已有异路推命未应验，则探索链直接阻断并顺延到 `prediction_until` 后，避免主动触发 `推命落空 -> 逆命劫 +1`。2026-06-28 继续补了 `docs/lab_tianxing_timeline_20260628.md` 与 `build_tianxing_timeline_plan(...)`：明确天星横切 `闭关/斗法/探索/炼制` 四条主链，`推命/改命` 需要统一时间线授权，不能只靠全局发送锁保序；同时收窄 `run_tianxing_scheduler()`，当前只允许查盘/观命/消劫/定命，`推命/改命` 不再由 scheduler 直接发送，避免和模块前置发生结构性竞态。2026-06-29 已补 `docs/lab_tianxing_development_plan_20260629.md`：明确 `确认发送 != 状态确认`，战略动作必须进入 `sent_waiting_ack -> state_confirmed -> downstream_released`，不确认不放行下游；规划 P1 纯规划器、P2 lab-only 执行状态机、P3 探索链确认式接入、P4 闭关 Farm 窗口、P6 UI。2026-06-29 已完成 P1/P2/P3 lab：`build_tianxing_timeline_plan(...)` 输出 `predict/change_fate/release_downstream` 步骤；新增 `tianxing_timeline_state` 与 `run_tianxing_timeline_scheduler(...)`，默认 dry-run，非 dry-run 仍走正常发送链；真实文案确认前不 release，发送失败不推进，超时只查盘校准并转重算；`探寻裂缝`/`野外历练` 不再直接发 `.定命/.改命`，只申请 timeline，拿到 `timeline_released` 才发送下游，异路推命冲突仍阻断。2026-06-29 已完成 P4/P6 lab：深度闭关只在 Farm 窗口、且即将发起时申请 `闭关` timeline；等待总结/被动结算不插天星动作；UI 快照和天星设置弹窗已展示时间线 phase、当前步骤、阻断、放行路线、audit，并可配置时间线、dry-run、Farm 窗口、确认超时和校准退避。验证：`.venv/bin/python -m pytest tests/test_tianxing.py tests/test_explore_rift.py tests/test_wild_training.py tests/test_phaseful_summaries.py tests/test_control_bool_coercion.py tests/test_persistence_runtime_flags.py tests/test_ui_dual_track.py -q` 184 passed / 12 subtests passed；`.venv/bin/python -m pytest tests/test_safety_watchdog.py tests/test_module_manifest.py tests/test_real_message_replay.py tests/test_app_scheduler_contract.py tests/test_message_evidence.py -q` 182 passed / 45 subtests passed；`compileall`、`node --check model/web/static/js/module_cards_ui.js`、`git diff --check` 通过。仍未 push、未上线、未做生产观察；lab 审计通过并已销号，后续如需合入生产需另行确认。 |
| 已销号 | 合欢双修温养出货三次提醒 | 天星审计结束后做：`.双修 温养` 成功文案若出现修为/宗门贡献之外的奖励，例如图纸、剑诀、功法等，立即 `@管理员`；同一出货事件再在 +3h、+6h 各提醒一次，共三次；需去重、可审计、不中断合欢原调度 | 修复 `model/features/hehuan.py`：温养成功若解析到 `共同领悟了【...】` 等非修为/宗贡奖励，写入 `valuable_drop_reminders` 队列；提醒按即时、+3h、+6h 三次发送 high priority 日志，自动带 `关注：@管理员`；同事件去重，发送失败 5 分钟后重试，不推进次数；每 tick 最多一条提醒，提醒发送当轮不继续发 `.双修 温养`，避免同轮叠发。测试：`.venv/bin/python -m pytest tests/test_hehuan.py tests/test_app_scheduler_contract.py tests/test_control_bool_coercion.py -q` 70 passed / 9 subtests passed；`py_compile model/features/hehuan.py` 与 `git diff --check` 通过。未上线、未 push、未重启。 |
| 已销号 | 钓鱼伴生机缘出货三次提醒 | 天星审计结束后做：本方账号钓鱼触发伴生机缘并产出宝物时，立即 `@管理员` 说明出什么；同一事件 +3h、+6h 复提醒，共三次；需只识别本方账号、去重、可审计 | 修复 `model/features/fishing_runtime.py`：仅在已路由到本身份的钓鱼回复中解析开鱼/伴生机缘奖励，普通灵石/灵鱼肉/灵鱼鳞/清灵草/修为不提醒；稀有关键词或伴生机缘非普通奖励写入 `fishing_valuable_drop_reminders` 队列，即时/+3h/+6h high priority 日志提醒，失败 5 分钟重试，每轮最多一条且当轮不再发送钓鱼游戏命令。补齐 `model/state.py`/`model/persistence.py` 字段与建表定义。吸收 rust 线“终态优先、事件去重、限次重试、不整块搬 planner”的安全设计。测试：`.venv/bin/python -m pytest tests/test_fishing.py tests/test_fishing_runtime.py tests/test_fishing_ui.py tests/test_storage_bag_transfer.py -q` 185 passed；`compileall` 与 `git diff --check` 通过。未上线、未 push、未重启。 |
| 已销号 | 探寻裂缝夺舍重生对照优化 | 天星审计结束后做：核对本仓库 `探寻裂缝` 是否已有夺舍重生机制，并对照 wxjerry/rust 线真实逻辑；没有则补，有则检查文案、补发、三层结构、审计和安全锁适配后优化 | 审计结论：本仓库已有夺舍重生状态机，包含大凶等待元婴遁逃、虚弱期结束后 `.夺舍重生`、解析三具肉身、仅自动选择“稳妥之身”、失败/无法定位转人工；rust 线仅做 snapshot/CD 发 `.探寻裂缝`，无夺舍恢复；wxjerry 线有 `【改命回天】` 终态和更多奖励/限制文案。安全吸收：`model/features/explore_rift.py` 新增 `【改命回天】` 终态收口，补 `平安带回/带回了/截下` 奖励上下文、修为未损/逸散解析、元婴期/分神限制拦截，避免天星改命后 pending 卡住。不吸收 wxjerry 按灵根排名自动夺舍，继续保守只选“稳妥之身”。测试：`.venv/bin/python -m pytest tests/test_explore_rift.py tests/test_tianxing.py tests/test_wild_training.py -q` 84 passed / 9 subtests passed；`py_compile` 与 `git diff --check` 通过。未上线、未 push、未重启。 |
| 已销号 | rust/wxjerry 元婴宗玩法调研与接入计划 | 天星报告交付后再研究 rust/wxjerry 的元婴宗相关玩法；只吸收安全、适配本仓库三层结构的设计，不整块搬运 | 调研结论：rust 线 `yuanying_cycle` 的可取点是元婴+按宗门分流，元婴宗发 `.元婴闭关`、其他身份发 `.元婴出窍`，8h10m 盲滚动；wxjerry 线有元婴闭关结算文案但结构弱于本仓库 phaseful。安全吸收：新增 `CMD_YUANYING_SECT_RETREAT=.元婴闭关`，纳入 `runtime` reply family、`control` 命令归属和 `action_guard` 同一个 `yuanying_launch` 会话；`model/features/yuanying.py` 按当前身份宗门选择启动/续轮/窍中温养命令，元婴宗用 `.元婴闭关`，其他保持 `.元婴出窍`。保留本仓库被动总结、summary_due、queued_launch、安全锁和等级读取结构，不搬 rust 盲滚动状态机。测试：`.venv/bin/python -m pytest tests/test_phaseful_summaries.py tests/test_message_evidence.py tests/test_startup_recovery_guards.py tests/test_module_manifest.py -q` 143 passed / 45 subtests passed；`py_compile` 与 `git diff --check` 通过。未上线、未 push、未重启。 |

补充销号记录（2026-06-28 02:15）：用户指出“全局那一排”仍未直接放在 logo/Xiuxian 同行。追补修复 `model/web/pages/index.html`：新增 `topbar-left`，将 `topbar-global-control` 从右侧 actions 移到 logo/Xiuxian 同组；修复 `model/web/static/css/ui_fixes.css` 与 `model/web_new/static/css/app.css`：顶栏 action 子项统一 `flex:0 0 auto/white-space:nowrap`，避免 `队列`、`官方定时` 注入按钮在窄屏压成竖排。验证：`.venv/bin/python -m pytest tests/test_ui_dual_track.py -q` 7 passed；`git diff --check` 通过；`tools/ui_http_smoke.py` 随机端口 13 checks ok；Chromium 1280x720 与窄屏量测显示 logo、全局、actions 均在 y=11/12 附近单行，actions 高度 30px。

补充销号记录（2026-06-28 02:28）：用户指出模块“设置”按钮不应单独占一行。追补修复 `model/web/static/js/module_cards_ui.js`：设置入口按钮直接拼接进 `module-tools-primary` 控制按钮组末尾；修复 `model/web/static/css/ui_fixes.css`：`.module-settings-entry` 改为普通 `inline-flex` flex item，不再右对齐成独立行。

| 已销号 | 2026-07-19 闯塔迁移 MiniApp | 闯塔已迁移至 MiniApp：新建独立 MiniApp 运行模块，复用现有“闯塔”UI 总开关、身份配置、执行窗口、冷却与成果展示；入口、请求、回包统一走 `webapp_core` 抓包/脱敏结构，按服务端进度与次数上限跑完。新旧链路不得并行抢跑；完成真实账号 Lab 对照、失败熔断、回包入账和回归测试后，删除旧文本命令状态机及其补发/状态查询路径，但保留历史状态迁移兼容。 | 新增 `tower_miniapp.py` 与洞府 `pagoda` 外部入口，自动链只执行 `start -> challenge`，绝不自动 `reset/重铸道心`；旧 `.闯塔` handler、summary replay、generic pending retry 已移除。全量 `3236 passed, 403 subtests passed`，2026-07-19 10:56 已部署。2026-07-22 分段入口适配上线后，`boxboxji` 真实 canary 仅产生一次 `start` 和一次 `challenge`，通过 21 层、止步 22 层、塔印 +51；生产日志无文本 `.闯塔` 发送，验收完成。 |
| 已销号 | 2026-07-19 钓鱼旧竿内文本自动链物理精简 | 公共 MiniApp 继续作为主动钓鱼出口；删除不可达的 `.钓鱼状态/.试探咬饵/.提竿/.收竿` 私有 followup/recovery 任务、定时补发和无主调度器恢复，不得只改日志或保留死代码。保留手工 `.钓鱼 <鱼塘> <鱼饵>` 回复兼容、鱼饵/鱼篓/开鱼解析、MiniApp 连钓、鱼获日结与赠送。 | `model/features/fishing_runtime.py` 已删除 `_FOLLOWUP_TASKS/_RECOVERY_TASKS` 及其 schedule/run/cancel 链，历史竿内 pending 只清理并退回公共 MiniApp，不调用 `send_game_command`；旧正向测试本体已删除，新增符号不存在和四类命令零发送回归。钓鱼定向 `221 passed, 10 subtests passed`，全量 `3236 passed, 403 subtests passed`；2026-07-19 10:56 已部署，重启后消息日志全日未见脚本 `.钓鱼状态/.提竿/.试探咬饵/.收竿`。 |
| 已销号 | 2026-07-19 吧唧小世界信仰/人口下降归因与指令策略复核 | 只读还原吧唧近期小世界面板、脚本主动命令、MiniApp 收割回包和天尊回复时间线，区分自然事件/被偷/神迹代价、显灵结果、布道/赈灾选择及收割香火造成的真实变化；重点复核近期指令优化是否把低损失场景错误选为布道、重复或过早执行神迹/赈灾、遗漏回复落账，或用旧用户名/旧状态做决策。未形成可复放证据前不主动探测、不改生产阈值。 | 真实 7 日证据见 `docs/audit/small-world-baji-attribution-20260722.md`：吧唧库存香火两次失窃 `-6550/-15564`，三次天灾信仰 `-11/-17/-13`，一次显灵失败信仰 `-6`、稳定 `-8`、人口 `-350`；后续面板人口均恢复 `250000/250000`，MiniApp 收割未见信仰或人口扣减。确认脚本缺口是信仰 `94/93` 仍花 8000 修为布道，现将文本链与公共入口统一改为仅 `<=85%` 例行布道，信仰未知 `0` 不误发；灾害和真实祈愿优先级不变。只读报告新增天灾、失窃、显灵、神迹绝对值事件及 `partially_explained`，真实区间识别 11 条神迹、3 条天灾、2 条失窃、7 条显灵，MiniApp 峰值 `56/90`。验证：focused `171 passed, 5 subtests passed`，全量 `3237 passed, 405 subtests passed`，compileall、diff check 通过；2026-07-22 18:59 重启后服务/health/watchdog 正常、`global_enabled=1`、pending 为空。 |
| 已销号 | 2026-07-19 天机阁可迁移命令研判与 Lab 测试 | 读取洞府公共入口 `commandCenter.entries` 与 `summary` 的真实回包，按 `integrated/external/groupOnly/locked/notMigrated/pending` 分类，对照本地命令目录和现有自动化，列出仍可从游戏群迁入 MiniApp 的候选。每个候选必须确认入口、身份切换、请求方法、幂等性、CD/资源字段、结果回包和失败语义；优先长 CD、面板可校准和只读动作，非幂等 POST 禁止传输层自动重试，`groupOnly/locked/notMigrated` 不强迁。 | 候选矩阵与 Gate 结论见 `docs/audit/tianjige-migration-matrix-20260722.md`：已按 integrated/external/groupOnly/locked/notMigrated 分类；真实 Lab 验证 `.我的阵法`、`.天阶状态`，并完成 `.我的阴罗幡`、`.我的侍妾` 只读 reducer 桥接；状态变更命令继续由原业务状态机持有，所有天机阁 POST 单次提交、未知传输不重放。研判任务完成；具体 `.天阶状态` 自动 Gate 保留为独立进行中事项，其他候选按需另开 Gate。 |
| 已销号 | 2026-07-20 野外历练无 CD 连续轮次与天星链路生产验收 | MiniApp 回包 `available=true` 且 `dailyRemaining>0` 时不得按 `24h/dailyLimit` 伪造 12 小时 CD，应短间隔进入下一轮；天星身份每一轮都必须重新确认未消费的 `.推命 探索`，首轮已消费推命不得复用，探索改命可按真实剩余时间保留。次数耗尽后必须停止动作并按服务端 `resetAt` 调度，禁止轮询风暴。 | 2026-07-29 跨日生产窗口完成验收：全局唯一 worker 将 18 个当前可运行身份全部完成 `2/2`，每身份连续轮次按服务端 `dailyRemaining` 约 60 秒后执行，身份间保持串行和最小间隔；未见旧事故中的同秒任务堆积、timeout、502、429 或身份缺失。WA 首轮后再次发送 `.推命 探索`，真实回复确认“已有一道探索推命尚未应验”后才放行第二轮，第二轮完成后按服务端次日 reset 排程。4 个频道冻结的天星身份因无法取得群内推命/改命证据而 fail-closed，未绕过前置直接跑公共入口。 |
| 已销号 | 2026-07-22 红包监听漏报与乱序撮合修复 | 复核源群金额不少于 50 的红包指令、成功卡、提醒渠道和直达链接；成功卡与指令可能由多监听同秒乱序到达，必须双向缓存撮合。提醒仍须成功卡事实证据、金额/数量一致、三次通知、双渠道及消息去重。 | 2026-07-29 00:40 真实命令 `.发红包 100 6` 与多行成功卡 `100.00 LDC / 6 份` 在 2 秒内正确撮合，00:40:49、00:40:52、00:40:56 共发送 3 次提醒，均使用成功卡消息直达链接 `https://t.me/ja_netfilter_group/458347/11715497`；同一窗口日志群与 Bot 通知渠道均无失败、超时或异常记录。00:49 的 `10 LDC / 5 份` 被 `below_threshold` 正确抑制。乱序撮合、阈值、三次双渠道提醒和去重完成生产验收。 |
| 补充销号 | 2026-07-22 红包真实多行成功卡解析 | 真实 `【LDC 红包】` 会把用户名与 `金额 LDC / 数量 份` 分成多行，旧正则因此记录为 `unparsed`，根本未进入双向撮合。 | `model/features/red_packet_monitor.py` 统一 NFKC 与空白归一化，保留金额/数量严格匹配和成功卡事实门槛；真实多行卡端到端测试已覆盖三次双渠道提醒及直达链接。focused `97 passed, 3 subtests passed`，全量 `3251 passed, 405 subtests passed`；提交 `e1f663cb`，19:47 上线后服务/watchdog active、pending=0；2026-07-29 真实多行成功卡已按上一行证据完成生产验收。 |
| 已销号 | 2026-07-22 wxjerry 最新线 MiniApp 差异吸收审计 | 通过 SSH 拉取 wxjerry 最新分支，重点对比其 MiniApp 目录、公共入口、请求/回包解析、身份入账与 UI 控制。第一优先级列出“wxjerry 已有、本地完全没有”的玩法或基础能力；第二优先级比较双方同功能实现，提取可降低漏回包、重复请求和维护成本的代码。禁止整块覆盖本地三层状态机，候选必须给出来源 commit、文件、适配边界、风险和 focused 测试后再上线。 | 已更新并审计当前 `origin/main=8ff9d90f`；历史结论见 `docs/audit/wxjerry-miniapp-diff-20260722.md`，本轮万兽谷 Gate A/B 证据见 `docs/audit/spirit-beast-miniapp-gate-ab-20260729.md`。洞府分段接口、灵树验轨、闯塔/野外迁移、天机试炼限次和持久化 P1 本地已等价或更完整；上游最新日志精简无可直接移植缺口。南宫阙生产 reducer/scheduler、青原子代理故障兜底、万兽谷 Gate C 和账号目标群成员门禁仍需独立 Lab，不整块 cherry-pick。 |
| 进行中 | 2026-07-25 旧链路/旧代码残留全局清理审计 | 全局盘点已迁移到 MiniApp 的旧文本 handler、follow-up/recovery 任务、定时调度器、旧 UI 开关、兼容别名和不可达状态迁移；按“仍需手工兼容 / 仅历史读取 / 已无调用者”分类，先做引用图和运行日志证据，再逐模块删除。不得直接按文件名清理，不得删掉频道身份兼容、历史状态迁移或手工命令入口；每项删除需 focused 回归、零发送回归、compile/diff-check、上线后观察和销号记录。 | 已新增 `tools/legacy_route_report.py` 和 `docs/audit/legacy-miniapp-route-reference-20260728.md`。钓鱼竿内自动发送、野外文本自动链、闯塔文本自动链和观星台主动文本 scheduler 已物理移除；灵树旧命令 flow 已归档。2026-07-30 继续退役 `.一键放养` 主动 scheduler、补发、静场延后和消息日志恢复，仅保留迟到回复/归来广播 parser，并将路由加入零发送报告。最近3天钓鱼/闯塔/野外/观星台/灵树/试炼/世界 Boss/放养旧链脚本发送均应为 0；仅小世界收割与 gated `.天阶状态` 保留兼容调用。当前没有足够证据删除这些 fallback、历史 parser、旧状态迁移或手工 UI；继续按对应 Gate 逐项清理。 |
| 已销号 | 2026-07-22 琉璃问心塔 MiniApp 动作与结算链路复核 | 正确入口为洞府公共入口的“外府 -> 琉璃问心塔”，进入后只执行一次“一念登塔”，随后等待服务端最终结算；不得沿用旧文本闯塔逐层循环、重复点击或主动重铸道心。需用真实回包确认按钮 action、请求路径、终态/编辑式结算、每日次数与成果字段，并确保超时只回捞、不重发非幂等登塔动作。 | 已确认外府动态入口为 `external:pagoda`；真实脱敏 capture 仅有 `/api/miniapp/xianxia-pagoda/start` 与 `/challenge` 各一次，`challenge` 回包直接包含完整 `replay`、最终 `state` 和奖励战报，等价于“一念登塔 -> 等结算”。传输层禁重试，自动链不调用 `/reset`；`boxboxji` 生产 canary 成功通过 21 层、止步 22 层、塔印 +51。 |
| 已销号 | 2026-07-23 外府天机命脉 MiniApp 自动化 | 外府新增“天机命脉”：进入玩法后保留页面全部默认选择，不主动改动选项；随后进入静室并执行一次结算，以服务端任务完成状态为准；最后返回天机命脉页面领取奖励，形成“进入 -> 默认选择 -> 静室结算 -> 返回领奖”的完整闭环。不得写死选项、次数、任务 ID 或奖励字段。 | 已落地显式命择 UI 与状态机：支持 `accept/hide`，高风险 `defy` 阻断；全局自动开关默认及现网均关闭。`draw/interpret/choose/meditation/settle` 全部单次 POST、禁 HTTP 重试，每次变更后 `/start` 回读校准；静室使用洞府 `/meditation`。2026-07-29 单身份 canary 完整闭环：首轮抽牌/解读/命择各 1 次，两个独立静室窗口各结算 1 次，最终领奖 1 次；所有变更均 HTTP 200、`attempt=1`，任务 `30/30 settled`，累计修为 `+36`、天机残痕 `+2`。旧记录 `meditation.gains/reward` 已兼容迁移到顶层累计且防重复。审计见 `docs/audit/fate-cards-miniapp-lab-20260728.md`；扩大身份范围仍需业务授权，不由销号自动开启。 |
| 已销号 | 2026-07-23 MiniApp 入口变化全局跟进 | 复核最新游戏 Bot、`startapp` 前缀、WebView 跳转页、MiniApp host/path、洞府公共入口及 `externalApps.groups[].apps[]` 动态应用清单的新旧结构；全局检查钓鱼、天机试炼、琉璃问心塔、观星台、灵树、野外历练、洞府寻宝、天机阁、世界 Boss 等已接入模块的入口 extractor、registry、身份切换与备用入口健康切换，避免只修某一个玩法。 | 当前协议变化已全局收口：共享入口层兼容 `account.externalApps.groups[].apps[]`、`externalApps.apps[]` 和平铺 `externalApps[]`，支持 bool/数字/字符串 available、相对 URL、key-only action 和 Bot shard 轮换；钓鱼、试炼、观星台、灵树、琉璃塔复用同一选择器。洞府 `/start -> /details` 动态目录会记录无 token/URL/initData 的安全 catalog，公共入口多 URL 仅在入口/会话健康失败时切换，业务 CD/次数终态不换入口。2026-07-29 试炼两波、野外跨日、钓鱼、观星台、灵树、元婴和深闭均使用当前动态入口正常运行；该轮入口迁移审计完成，后续新协议变化按新 incident 处理。 |

## 已知已销号但保留追踪

| 已销号 | 2026-07-11 WA 探寻裂缝重复发送倒查 | 用户观察 WA `.探寻裂缝` 发了两次，怀疑首次 CD 回复已到但状态机未采纳。只读核对两次发送消息 ID、天尊 reply/edit、监听账户、message-log recovery、pending/retry 与天星放行记录；若首次 CD 已存在，必须通过正常 handler 回放推进真实冷却并修复重试门禁，禁止用延长退避掩盖。排查期间不主动发送裂缝命令。 | 已完成共享 message-log recovery、早期回包回放和天星消费门禁复核。2026-07-15 WA 仅在 00:03:04（msg 139031）与 12:06:25（msg 156650）发送两次，间隔 12 小时 3 分；两次均在 1-13 秒内收到起始消息及最终 edit，分别结算四级妖丹 x2、x1，没有同一冷却窗口重复发送。 |

| 状态 | 事项 | 销号记录 |
| --- | --- | --- |
| 已销号 | AI 答题接入、多供应商投票、模型获取/手填 | 已在前序提交落地；测试覆盖 `tests/test_quiz_absorb.py` |
| 已销号 | 储物袋赠送与转移补发加速 | 已在前序提交落地；测试覆盖 `tests/test_storage_bag_transfer.py`、`tests/test_storage_bag_api.py` |
| 已销号 | 第二元神魔染 >=90 自动镇魔 | 已在前序提交落地；测试覆盖 `tests/test_second_soul.py` |
| 已销号 | 世界 boss 风暴发送错误命令 | 已在前序提交落地；禁用无事件主动战况探测 |
| 已销号 | UI 响应式与角色卡基础字段 | 已在前序提交落地；本批次仅补命令读取来源 |

补充销号记录（2026-06-28 13:24）：用户追问“储物袋咋又超时了”。根因：赠送阶段目标号定位消息留下 20s `reply_due_at`，随后来源号 `.赠送` 在发送队列等待时继承旧超时，scheduler 在真实赠送命令返回 message_id 前将任务判为 `gift_sending` 超时；成功回包随后只能落入通用赠送库存分支，UI 误判任务失败。修复 `model/features/storage_bag.py`：储物袋链式命令发送前清空上一阶段 `reply_due_at`，等真实发送成功后再按 20s/5s 重新计时。补 `tests/test_storage_bag_transfer.py::StorageBagTransferExecutionTests::test_gift_command_send_pending_does_not_inherit_locator_timeout` 覆盖发送 pending 期间 scheduler 不会误超时。验证：`.venv/bin/python -m pytest tests/test_storage_bag_transfer.py -q` 53 passed；安全锁/真实赠送 focused 3 tests passed；`py_compile`、`git diff --check` 通过；上线重启后 `health_observer ok`、`watchdog ok`、`global_enabled=1`、无 fused；提交 `c9e3b76 Prevent storage gift send timeout inheritance` 已推 `xiuxian-mian/main`。

补充销号记录（2026-07-16 01:12）：婉心委托跨监听回包死循环已复现并修复。WA 委托 99 的接取命令 msg 169757 在 22:50:49 收到成功回包 msg 169760，但回包由另一 listener 收到，且游戏文案用户名 `WalterWA2000` 与本地 profile `WalterWA20000` 不完全一致；旧 handler 因用户名和当前监听身份均不匹配，未把 `accepted=1` 写回，随后 23:07、23:18、23:30、23:42 重复 `.接取解咒委托 99`。修复 `model/features/wanxin.py`：用户名匹配失败时按精确 `reply_to_msg_id` 跨监听回找 owner pending，并关闭实际阴罗协助身份的 action guard；新增回归 `test_accept_contract_recovery_uses_reply_anchor_across_listener_and_aliases`。验证：`tests/test_wanxin.py` 40 passed；线上证据回放后 WA 委托 99 为 `accepted=1`、`accept_msg_id=169760`、pending 清空，服务恢复后未再出现重复接取；提交 `9912e4b3 Recover Wanxin contracts across listeners` 已推送。

补充销号记录（2026-07-16 09:20）：落云灵树 7 月 16 日未运行的根因是频道身份因 `SendAsPeerInvalidError` 被健康模块冻结后，`tree_runtime.check_tree_miniapp_eligibility()` 仍使用普通身份总开关判断，导致已配置的落云宗 `imcanonical_ai` 被跳过；公共洞府入口本身可用，且其他洞府 MiniApp 已通过 `is_cave_public_identity_available()` 放行“仅频道发送被冻结”的身份。修复灵树资格门禁统一使用该公共入口可用性判断，仍拒绝真正手动停用的身份，不恢复频道群发。新增回归覆盖频道健康冻结可运行及手动停用继续阻断。验证：灵树/公共入口定向 `176 passed / 10 subtests`，全量 `3104 passed / 396 subtests`，`compileall`、`git diff --check` 通过；待重启后观察当天灵树公共入口自动补跑并记录结果。

补充整改记录（2026-07-16 11:05）：资格门禁修复上线后，落云灵树自动补跑暴露飞一飞服务端校验差异：客户端按 `1000/60ms` 重放得到 17 分，服务端仅确认 8 分，运行层按 fail-closed 在首次样本后停止，保留飞行剩余 2 次。使用已消耗样本的 seed、32 个 flap 时间戳和 23067ms proof 离线复盘，确认固定 `16ms` 重放恰好得到服务端 8 分；因此校准飞行规划/重放步长为 `16ms`，保留服务端分数不一致即阻断。按用户目标将跳一跳随机目标调整为 30-36、飞一飞调整为 8-12，跳跃安全上限提高到 45，并从 30 分起启用高精度落点规划；新增真实样本回放回归和 UI 区间回归。当天跳一跳已用完 3/3，不再补发；飞一飞在全量测试和配置迁移前继续阻断，不拿剩余次数试错。
