# 旧钓鱼发送快速通道退役审计（2026-07-28）

## 结论

主动钓鱼已由洞府公共 MiniApp 接管，旧文本链不再需要共享发送层的快速间隔特权。本轮移除 `.钓鱼状态`、`.试探咬饵`、`.提竿`、`.收竿`、`.开鱼`、`.鱼篓` 及 `灵溪垂钓` 模块的快速白名单。

## 保留边界

- 保留旧文本回复解析，用于手工命令、历史消息回放、未完成状态清理和渔获统计。
- 保留命令族映射与 watchdog 样本，使误发、重复发送和历史风暴仍可被识别。
- 主动调度继续只执行 MiniApp 流程；入口未接管或不可用时保守停止，不回退旧文本钓鱼链。
- 储物袋转移链的快速白名单与 5 秒模块间隔不变。

## 验证

- 聚焦：`212 passed, 23 subtests passed`。
- 全量：`3371 passed, 535 subtests passed`。
- `python -m compileall -q model tests`、`git diff --check` 通过。

## 生产结果

- 提交 `45867cd1` 已推送至 `xiuxian-mian/main`，2026-07-28 03:51 UTC+8 重启 `xiuxian.service`。
- 启动后恢复 24 个身份，pending 队列为空，watchdog 正常。
- 上线观察窗口内未出现脚本主动发送上述旧钓鱼命令；已知 listener sidecar 未独立授权告警保持不变，主运行时监听正常。
